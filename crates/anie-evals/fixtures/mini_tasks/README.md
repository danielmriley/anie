# mini_tasks

A tiny task-list app used as a cold-start eval fixture: the agent
has never seen this code, so navigation must come from the repo
map (or exploration, with the map off).

- `src/app.py` — task store and lifecycle.
- `src/util.py` — small string helpers.
- `web/index.ts` — render layer.
