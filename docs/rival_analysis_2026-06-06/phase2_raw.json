{
  "summary": "Grade anie against rival coding agents across 8 lenses, then adversarially verify every gap claim against the actual code",
  "agentCount": 56,
  "logs": [],
  "result": {
    "counts": {
      "confirmed_gap": 33,
      "partial": 11,
      "already_exists": 4,
      "overstated": 0,
      "unverified": 0
    },
    "confirmed_gap": [
      {
        "id": "APPROVAL-1",
        "lens": "sandbox-approvals",
        "title": "No approval/permission prompts before tool execution",
        "severity": "high",
        "impact": 5,
        "effort": 4,
        "corrected_effort": 5,
        "reasoning": "The claim is confirmed. anie has zero tool-execution approval/permission gating in production code:\n\n**Existing Scaffolding (Plumbing Only):**\n- BeforeToolCallHook trait with Block variant exists at crates/anie-agent/src/hooks.rs:39-46, but is pub(crate)-scoped and test-only\n- BeforeToolCallResult enum with Allow/Block variants exists at crates/anie-agent/src/hooks.rs:19-24\n- AgentLoopConfig field `before_tool_call_hook: Option<Arc<dyn BeforeToolCallHook>>` at line 369\n- Hook invocation logic at crates/anie-agent/src/agent_loop.rs:1466-1486 that checks result and blocks on Block variant\n- with_hooks() method at crates/anie-agent/src/agent_loop.rs:489-497 marked #[cfg(test)]\n\n**No Production Instantiation:**\n- grep for 'impl BeforeToolCallHook' outside tests.rs returns only test-only BlockingHook implementation (crates/anie-agent/src/tests.rs:250-260)\n- AgentLoopConfig::new() initializes before_tool_call_hook to None (line 403)\n- No controller path in crates/anie-cli/src/controller.rs calls with_hooks() or sets the hook\n- README.md:39 explicitly documents: 'tools currently run without sandboxing or approvals'\n\n**Why This Is a Gap:**\n- The trait is structural plumbing for a design not yet realized\n- No event loop integrates the hook with user decision-making (no approval modal, no TUI approval UI, no permission prompts)\n- No AgentEvent variant exists for approval requests (grep for ApprovalRequest/ApprovalEvent returns zero hits)\n- BashPolicy pre-spawn deny enforcement (crates/anie-tools/src/bash.rs:234-265) is a guardrail only, not approval-gated, and is explicitly documented as bypassable\n\n**Future Work Acknowledged:**\n- Plan 10 (docs/refactor_plans/10_extension_system_pi_port.md) phases 3+ design blocking events with before_tool_call as an extension hook fired to external processes, returning approval decisions\n- This is planned as out-of-process JSON-RPC, not a TUI modal integrated into the current agent loop\n\n**Contrast to Claude Code:**\n- Claude Code has settings.permissions with defaultMode supporting 'ask'/'allow'/'deny'/'auto' modes with actual modal UI and user approval flow\n- Codex has ExecPolicy evaluation with approval request events and TUI modal handling\n- anie has zero parallel structure: no settings mode, no approval modal, no event emission, no user-facing approval decision point\n</reasoning>\n<parameter name=\"corrected_impact\">4",
        "claim": "anie has zero tool-execution approval/permission gating: BeforeToolCallHook trait exists (with Block variant at crates/anie-agent/src/hooks.rs:22), but no implementation is ever instantiated in non-test code, and no controller path installs it.",
        "rival_baseline": "Claude Code: settings.permissions.defaultMode supports 'ask', 'allow', 'deny', 'auto' modes (visible in ~/.claude/settings.json). Codex: ExecPolicy evaluation with 'Allow'/'Deny'/'AskForApproval' decisions; approval flow emits ExecApprovalRequestEvent, TUI shows modal, user can approve once/session/session-with-rule (codex_summary.md:192-198)."
      },
      {
        "id": "SANDBOX-1",
        "lens": "sandbox-approvals",
        "title": "No process sandbox for bash tool execution",
        "severity": "critical",
        "impact": 5,
        "effort": 5,
        "corrected_impact": 4,
        "corrected_effort": 3,
        "reasoning": "The claim is accurate and well-documented in the codebase. Evidence confirms:\n\n1. **bash.rs:22 comment** — Verified at `/home/daniel/Projects/agents/anie/crates/anie-tools/src/bash.rs:22`: \"The command is not sandboxed.\" This is explicitly stated in the struct docstring.\n\n2. **No platform-specific sandboxing** — Confirmed. The crate has zero imports of seccomp, landlock, bubblewrap, pledge, or capsicum crates. The only nix crate usage (lines 356-357) is for signal-sending (SIGKILL), not sandboxing. Cargo.toml lists only standard isolation tools: async-trait, tokio, regex, nix (for signals only).\n\n3. **Unsandboxed command spawning** — Verified at lines 104-109: `Command::new()` with `.current_dir()` and `.kill_on_drop(true)`. Line 111 adds `.process_group(0)` on Unix for process group cleanup, not isolation.\n\n4. **SIGKILL-only termination** — Confirmed at lines 349-365: `kill_process_tree()` uses only `nix::sys::signal::Signal::SIGKILL` on Unix and `taskkill /F /T` on Windows. No seccomp filter, rlimit, namespace enforcement, or capability dropping.\n\n5. **Bash deny policy is explicitly NOT a sandbox** — Verified at `/home/daniel/Projects/agents/anie/docs/arch/anie-rs_architecture.md:459-463`: \"This is an accidental-risk guardrail, not a security boundary. It is useful for user preferences like 'never run `rm`' or 'never force-push', but it is bypassable via shell indirection, scripts, interpreters, or future non-bash tools.\"\n\n6. **Architecture document confirms design choice** — Lines 27-31 of anie-rs_architecture.md: \"The project currently chooses simplicity over isolation: tools intentionally run in the current process user's security context without sandboxing or approval prompts.\"\n\nThis is a genuine, intentional architectural gap, not an oversight. The project explicitly defers process sandboxing to a future WASM/containerized tool-execution layer (line 30-31).\n\nUnlike Codex's multi-layer approach (Landlock + bubblewrap on Linux, restricted tokens on Windows, filesystem bind-mount isolation), anie has zero layers of process isolation.",
        "claim": "bash.rs:22-25 explicitly documents 'The command is not sandboxed' and has no platform-specific sandbox wrapping (no Landlock, bubblewrap, restricted tokens, or seccomp). Bash processes spawn with anie's full user context and SIGKILL-on-drop is the only isolation.",
        "rival_baseline": "Codex (codex_summary.md:152-159): Shell commands go through ExecPolicy → SandboxManager → PTY spawning. Platform-specific: linux-sandbox/ uses Landlock+bubblewrap (line 38-39, 53). windows-sandbox-rs/ uses Windows restricted tokens. macOS uses seatbelt/sandbox policy. All subprocess I/O proxied through network-proxy."
      },
      {
        "id": "BYPASS-1",
        "lens": "sandbox-approvals",
        "title": "Bash deny policy is bypassable guardrail, not a security boundary",
        "severity": "medium",
        "impact": 2,
        "effort": 1,
        "corrected_impact": 3,
        "corrected_effort": 5,
        "reasoning": "The bash deny policy in anie is correctly documented and implemented as a bypassable guardrail. The codebase explicitly acknowledges this in multiple places: bash.rs:28-31 (\"accidental-risk guardrail, not a sandbox\"), README.md:188 (\"not a sandbox\"), and architecture docs. The command_names extraction (bash.rs:268-293) only splits on simple delimiters and skips wrapper commands, but does not parse or detect shell metacharacters like heredocs, command substitution, subshells, or process substitution. Verification: tested code paths use only textual regex/command-name matching with no AST-level parsing. No tests exist for bypass scenarios. The proper solution is acknowledged as future WASM/containerized tool execution (anie-rs_architecture.md), which is not yet implemented. This is not a bug — it's the intended design — but it IS a genuine architectural gap that requires future work to address if security-grade isolation is needed.",
        "scaffolding": "/home/daniel/Projects/agents/anie/crates/anie-tools/src/bash.rs:28-52 (BashPolicy struct and documentation), bash.rs:233-265 (check method with pattern matching), bash.rs:268-293 (command_names parsing with simple split)",
        "claim": "BashPolicy (bash.rs:28-52) is documented in README:188 as 'not a sandbox and should not be treated as a security boundary.' Policy checks raw command strings before fork; easily bypassed via shell indirection, scripts, interpreters (sudo, env, nohup, time are explicitly skipped at bash.rs:288).",
        "rival_baseline": "SPECULATIVE — Codex likely uses the same deny-check approach for fast filtering (codex_summary.md not explicit on bypass resistance), but wraps execution in platform sandbox so bypass has no security impact. Claude Code: unknown mechanism for deny-list enforcement from visible code."
      },
      {
        "id": "MCP-GAP-1",
        "lens": "mcp",
        "title": "No MCP client implementation; tools are compile-time registered only",
        "severity": "high",
        "impact": 4,
        "effort": 4,
        "corrected_impact": 4,
        "corrected_effort": 5,
        "reasoning": "Comprehensive codebase search found zero MCP client infrastructure in anie. The ToolRegistry is a simple HashMap populated at startup from hardcoded tools (crates/anie-agent/src/tool.rs:145-157). Tool registration only happens via build_tool_registry_with_policy() in bootstrap.rs:131-186, which calls Arc::new(ReadTool::new(...)) style patterns with no extensibility. No ToolRegistry.register() calls exist outside bootstrap. Zero MCP-related crates found in workspace. No mcp_servers config field in AnieConfig (crates/anie-config/src/lib.rs). Only OAuth scope mention found (crates/anie-auth/src/anthropic_oauth.rs:55), indicating the credential infrastructure exists but is unused for MCP. Docs reference Codex's codex-mcp / rmcp-client / mcp-server, proving anie lacks what Codex has. This is a genuine feature absence, not scaffolding.",
        "scaffolding": "none",
        "claim": "anie has zero runtime extensibility for external MCP servers. All tools are hardcoded and registered at startup via `build_tool_registry_with_policy()` (crates/anie-cli/src/bootstrap.rs:131-186). There is no `McpManager`, `McpConnectionManager`, or `rmcp-client` equivalent. Tools cannot be added dynamically from stdio/HTTP servers.",
        "rival_baseline": "Codex has `codex-mcp` (McpManager + McpConnectionManager), `rmcp-client` (HTTP/stdio/OAuth MCP client), and `mcp-server` (serves Codex itself as an MCP server). Claude Code natively speaks MCP per system-reminder. This is table-stakes for modern agent extensibility."
      },
      {
        "id": "MCP-GAP-2",
        "lens": "mcp",
        "title": "Tool `ToolDef` schema is static; no runtime schema discovery or resources protocol",
        "severity": "high",
        "impact": 3,
        "effort": 3,
        "corrected_impact": 5,
        "corrected_effort": 4,
        "reasoning": "Verified absence of MCP resources protocol support in anie: (1) ToolDef is a fixed 3-field struct (name, description, parameters:JSON) defined at crates/anie-protocol/src/tools.rs:7-14, not in messages.rs as claimed. (2) No runtime schema discovery code exists—grep found zero hits for ListResourcesRequest, ReadResourceRequest, resources/list, resources/read. (3) Tool trait at crates/anie-agent/src/tool.rs:111 returns static ToolDef via fn definition(&self) -> ToolDef; cached once at registration time via rebuild_sorted_definitions() at line 216-220. (4) System prompt renders tool list as flat strings at crates/anie-cli/src/controller.rs:2080-2085: `map(|tool| format!(\"- {}: {}\", tool.name, tool.description))` — no schema introspection, no resource discovery. (5) MCP is mentioned only in OAuth scopes (crates/anie-auth/src/anthropic_oauth.rs) and future Codex architecture plan (docs/arch/codex_summary.md line 284-294), but zero implementation in current anie codebase. (6) No external server process spawning for tool discovery; ToolRegistry holds pre-compiled validators at registration only. This is a confirmed gap: anie lacks both dynamic tool schema discovery and MCP resources protocol entirely.",
        "scaffolding": "none",
        "claim": "anie's `ToolDef` (crates/anie-protocol/src/messages.rs) is a fixed schema defined at compile time. There is no support for MCP resources protocol (files, directories, image/text URIs), no dynamic schema detection from external servers, and no resource listing/fetching via `resources/list` or `resources/read` JSON-RPC calls.",
        "rival_baseline": "SPECULATIVE: MCP spec (https://modelcontextprotocol.io/) defines resources/ as a first-class protocol alongside tools/ and prompts/. Servers expose ListResourcesRequest, ReadResourceRequest for URIs like file://, dir://, image://. Codex's `rmcp-client` and tools integration would support this."
      },
      {
        "id": "MCP-GAP-3",
        "lens": "mcp",
        "title": "No MCP prompts protocol; system-prompt composition is hard-coded",
        "severity": "medium",
        "impact": 2,
        "effort": 3,
        "corrected_impact": 3,
        "corrected_effort": 4,
        "reasoning": "anie has no MCP prompts/ protocol support for external system-prompt injection. The system prompt is built from a hardcoded template (crates/anie-cli/src/controller.rs:2075-2106) that composes from: default instructions, project context files (AGENTS.md/CLAUDE.md), and RLM augmentation. There is no hook to query external servers for dynamic prompt templates. A BeforeModelPolicy hook exists (crates/anie-agent/src/agent_loop.rs:342) allowing AppendMessages() to context BEFORE model calls, but this injects conversation messages, not system prompts, and requires in-process implementation, not external MCP servers. No MCP server implementation exists in anie; the only MCP reference is an OAuth scope mentioning Anthropic's mcp_servers endpoint. The claim is accurate: system-prompt composition is hardcoded with no external provider pluggability.",
        "scaffolding": "crates/anie-agent/src/agent_loop.rs:342 (BeforeModelPolicy trait) — allows context message injection before model calls, but not system prompt composition and not from external sources",
        "claim": "System prompts are built from a hardcoded template (crates/anie-cli/src/controller.rs:2075-2106) plus project-context files. There is no support for MCP's prompts/ protocol, which allows servers to inject dynamic instructions, templates, or context into the agent's system prompt at runtime.",
        "rival_baseline": "SPECULATIVE: MCP's prompts/ protocol allows servers to expose ListPromptsRequest, GetPromptRequest with name, arguments, and templates. A Codex prompt server could inject per-context system messages. anie has no equivalent."
      },
      {
        "id": "MCP-GAP-5",
        "lens": "mcp",
        "title": "No OAuth credential bridging for MCP servers requiring auth",
        "severity": "high",
        "impact": 3,
        "effort": 3,
        "corrected_impact": 4,
        "corrected_effort": 3,
        "reasoning": "The `user:mcp_servers` scope is requested in crates/anie-auth/src/anthropic_oauth.rs:55, confirming architectural intent. However, search of the entire codebase found: (1) Zero MCP client code or `rmcp` dependency, (2) No MCP server initialize/ClientInfo/ServerInfo types, (3) No code path injecting OAuth tokens into downstream MCP RPC calls, (4) Credentials retrieved only for model discovery API calls (github-copilot's api_base_url routing), never for MCP initialization. The scope request and credential storage infrastructure exist (scaffolding), but there is no MCP protocol implementation, no server discovery/registration, and no mechanism to bridge OAuth credentials to external MCP servers. This is a genuine architectural gap.",
        "scaffolding": "crates/anie-auth/src/anthropic_oauth.rs:55 (scope request), crates/anie-auth/src/store.rs (credential storage with refresh locking), crates/anie-auth/src/refresh.rs (token rotation)",
        "claim": "OAuth is implemented for five cloud providers (Anthropic, OpenAI Codex, GitHub Copilot, Google Antigravity, Gemini CLI). The OAuth scope `user:mcp_servers` is requested (crates/anie-auth/src/anthropic_oauth.rs:55) but anie has no code path to use that credential to authenticate downstream MCP servers. There is no MCP `initialize` with Client/Server metaData auth fields, no token refresh, and no credential injection into MCP RPC calls.",
        "rival_baseline": "Codex `rmcp-client` would accept OAuth access tokens and inject them into MCP `initialize` ClientInfo.auth or per-call Authorization headers. anie's scope request suggests this was planned but unimplemented."
      },
      {
        "id": "MCP-GAP-6",
        "lens": "mcp",
        "title": "Tool registry is not extensible at runtime; no plugin discovery or hotloading",
        "severity": "medium",
        "impact": 2,
        "effort": 4,
        "corrected_impact": 3,
        "corrected_effort": 5,
        "reasoning": "The claim accurately describes anie's current architecture: ToolRegistry is created once at startup (build_tool_registry_with_policy in crates/anie-cli/src/bootstrap.rs:136-186), wrapped in Arc::new() and becomes immutable thereafter. The register() method requires &mut self (crates/anie-agent/src/tool.rs:171), making it impossible to add tools after Arc wrapping. AgentLoop takes Arc<ToolRegistry> with no mutation methods (crates/anie-agent/src/agent_loop.rs:726-745). RLM mode creates per-run registries (crates/anie-cli/src/controller.rs:1789-1798) but only to inject the recurse tool - a one-time operation at run creation, not a runtime extension point. There is no manifest discovery, no directory scanning, no .so/.dll loading, and no MCP server integration (only an OAuth scope reference with no implementation). However, this gap is documented as a planned feature (Plan 10 in docs/refactor_plans/10_extension_system_pi_port.md, 804 lines) with detailed phase breakdown. The plan specifies subprocess-based JSON-RPC extensions with manifest discovery at ~/.anie/extensions/ and ./.anie/extensions/. It is listed in ROADMAP as \"Multi-week; blocked on OAuth for phase 7, otherwise ready to start\" — not an overlooked gap but a deferred architectural addition.",
        "scaffolding": "docs/refactor_plans/10_extension_system_pi_port.md:1-803 (comprehensive plan for 7-phase extension system with manifest discovery, JSON-RPC transport, tool/command/shortcut registration, and UI integration). No code scaffolding exists yet (anie-extensions crate not created).",
        "claim": "ToolRegistry is built once at startup and is immutable thereafter. Sub-agents create a new registry but cannot share external server connections. There is no plugin discovery (glob, directory scan), no .so/.dll loading, and no daemon pattern for long-lived server processes. Tools cannot be added after the agent loop starts.",
        "rival_baseline": "Codex's tools crate registers MCP tools into the same unified registry. Plugins and MCP servers are discovered at startup and can be re-scanned. anie has no equivalent scanning or discovery mechanism."
      },
      {
        "id": "EXT-1",
        "lens": "extensions-skills-hooks",
        "title": "Plan 10 Phase 1 (JSON-RPC Transport) is 100% unstarted",
        "severity": "high",
        "impact": 5,
        "effort": 5,
        "corrected_impact": 5,
        "corrected_effort": 5,
        "reasoning": "Plan 10 Phase 1 (JSON-RPC Transport for extension subprocesses) has zero implementation. Verified via: (1) No anie-extensions crate exists in workspace (deleted in Plan 07 as precondition); (2) No manifest.rs, runner.rs, host.rs files in any crate; (3) Zero grep matches for JsonRpcRequest, ExtensionRunner, ExtensionHost, ExtensionManifest; (4) No jsonrpc/jsonrpsee workspace dependency; (5) Official status in docs/refactor_plans/README.md explicitly marks Plan 10 as 'Not started'; (6) Plan doc created 2026-04-17, ~7 weeks ago with zero implementation commits since. The in-process hooks (BeforeModelPolicy, BeforeToolCallHook/AfterToolCallHook) and RecurseTool exist but are NOT the JSON-RPC transport this plan requires. Plan 10 intends to rebuild anie-extensions from scratch with subprocess JSON-RPC, not in-process trait-based hooks.",
        "scaffolding": "anie-agent/src/hooks.rs:39-59 (BeforeToolCallHook/AfterToolCallHook traits, pub(crate) only), anie-agent/src/agent_loop.rs (BeforeModelPolicy hook integration), anie-tools/src/recurse.rs (RecurseTool for sub-agent spawning)",
        "claim": "Plan 10's Phase 1 — manifest discovery, ExtensionRunner, JSON-RPC envelope, subprocess spawning — has zero implementation. No anie-extensions crate exists; no manifest.rs, runner.rs, or host.rs files. The plan is exhaustively detailed but untouched.",
        "rival_baseline": "Claude Code (pi): `packages/coding-agent/src/core/extensions/` is 3099 LOC with full JSON-RPC 2.0 subprocess transport, manifest loading from ~/.pi/extensions/, dynamic discovery, and routing. Codex: MCP servers (subprocess JSON-RPC) for tool/capability registration; 5 lifecycle hooks via codex-hooks crate (session_start, pre_tool_use, post_tool_use, etc.). Both rivals ship working extensibility; anie does not."
      },
      {
        "id": "EXT-2",
        "lens": "extensions-skills-hooks",
        "title": "Skills system not implemented; SlashCommandSource::Skill is a stub",
        "severity": "high",
        "impact": 3,
        "effort": 3,
        "corrected_impact": 3,
        "corrected_effort": 4,
        "reasoning": "The claim is confirmed: the skills system has zero runtime implementation. Code findings:\n\n1. **Variant is defined but never instantiated:** SlashCommandSource::Skill { skill_name: String } (anie-tui/src/commands.rs:35) is a stub. Grep across all .rs files finds only pattern-matching in display logic (label(), grouped_by_source()) but zero construction sites (no SlashCommandSource::Skill { skill_name: \"...\" } anywhere).\n\n2. **No skill loader exists:** Zero grep hits for:\n   - `read_dir` on `.anie/skills/` or `~/.anie/skills/`\n   - `read_to_string` of `SKILL.md` files\n   - YAML/frontmatter parsing (no `serde_yaml` use in anie crates; frontmatter code that exists is for web-read tool output only, not skill discovery)\n   - Any function named `load_skill`, `discover_skills`, `parse_skill_frontmatter`\n   - skill_name being used in any dispatch path\n\n3. **No dispatch handler:** anie-tui/src/app.rs::dispatch_validated_command (line 1312+) matches on `info.name` for all ~20 builtin commands (model, thinking, context-length, clear, session, onboard, providers, copy, markdown, tool-output, login, logout, quit) but has zero handler for any skill-sourced command. The default arm (line 1472) would surface a \"no handler\" error if a skill command were somehow registered.\n\n4. **Skills exist in `.claude/skills/` as documentation, not as runtime-discoverable plugins:**\n   - /.claude/skills/adding-providers/SKILL.md (exists, human-readable)\n   - /.claude/skills/live-provider-smoke/SKILL.md (exists, human-readable)\n   - These are reference materials for the human developer, not loaded by anie at runtime.\n\n5. **Documented as planned, not shipped:** docs/ROADMAP.md item 10 (Longer-Term — Features, not completed) explicitly lists \"Skills system\" as \"Medium effort — file loading, frontmatter parsing, command registration\" with \"not yet started\" status. docs/notes/skills_system.md is 70 LOC of specification with no implementation date.\n\n6. **Predecessor work exists but is incomplete:** Plan 03 phase 3 (1e0819a, 2025-01-15) added SlashCommandSource::Skill as part of the slash-command metadata registry refactor, explicitly noting in comments that the variant \"will be constructed once extensions (plan 10) and prompt/skill loaders land.\" Plan 10 (Extension system pi-shaped port) exists at docs/refactor_plans/10_extension_system_pi_port.md and is blocked on OAuth (phases 1–3 ready, phase 7 blocked).",
        "scaffolding": "anie-tui/src/commands.rs:35 (SlashCommandSource::Skill variant definition); anie-cli/src/commands.rs:91–92 (grouped_by_source placeholder); anie-tui/src/app.rs:73 (commands field in App struct, includes skill metadata) — all display/organization only, zero execution path",
        "claim": "The Agent Skills standard (SKILL.md files with YAML frontmatter, /skill:name commands) is documented in docs/notes/skills_system.md but has zero implementation. SlashCommandSource::Skill variant (anie-tui/src/commands.rs:35) is defined but never instantiated. No skill loader, no SKILL.md parser, no /skill command handler exists.",
        "rival_baseline": "Claude Code (pi): Supports Agent Skills standard (https://agentskills.io/) — skills are loaded from ~/.pi/skills/, front-matter parsed, registered as /skill:name commands with tool restrictions via allowed-tools field. Codex: Skills are mentioned as injectable into initial context but implementation not detailed in the summary; assumed less mature than pi. Grok Build: SPECULATIVE but likely similar to pi."
      },
      {
        "id": "EXT-3",
        "lens": "extensions-skills-hooks",
        "title": "Tool-call hooks are pub(crate) — not public extensibility API",
        "severity": "medium",
        "impact": 2,
        "effort": 5,
        "corrected_impact": 4,
        "corrected_effort": 3,
        "reasoning": "Tool-call hooks (BeforeToolCallHook, AfterToolCallHook) are strictly pub(crate) in anie-agent/src/hooks.rs (lines 39, 51), with no public extensibility API. Module documentation (hooks.rs lines 2-9) explicitly confirms this is intentional: \"These traits are pub(crate) by design...A public extension API is planned separately as an out-of-process JSON-RPC system.\" Public exports in anie-agent/src/lib.rs (lines 9-13) include BeforeModelPolicy and CompactionGate but deliberately omit hook traits. The only method to install hooks—AgentLoopConfig::with_hooks (agent_loop.rs:487-497)—is marked both #[cfg(test)] and pub(crate). Hooks are only used in the test module (tests.rs), confirming they are testing infrastructure. Plan 10 (docs/refactor_plans/10_extension_system_pi_port.md) describes the deferred JSON-RPC replacement. External code has zero mechanism to intercept tool calls in-process or otherwise; rivals (pi, Codex, Grok) all expose tool hooks as inter-process events. Existing scaffolding (BeforeModelPolicy, CompactionGate, Tool trait) cannot substitute—they operate at model-request or tool-result boundaries only, not call-time interception.</reasoning>\n<parameter name=\"existing_scaffolding\">anie-agent/src/agent_loop.rs:265-346 (CompactionGate, BeforeModelPolicy traits for model-request interception; insufficient for tool-call hooks)",
        "claim": "BeforeToolCallHook and AfterToolCallHook traits (anie-agent/src/hooks.rs:39–59) are explicitly pub(crate), blocking external implementations. The hooks.rs module comment (lines 2–9) confirms this is intentional: 'These traits are `pub(crate)` by design ... A public extension API is planned separately as an out-of-process JSON-RPC system.' External code cannot implement hooks; only internal consumers can.",
        "rival_baseline": "Claude Code (pi): Extension system makes blocking/overriding decisions via JSON-RPC events (before_agent_start, before_tool_call, after_tool_call, context) returned by extensions; hooks are not traits but RPC responses. Codex: pre_tool_use / post_tool_use hooks are external commands (JSON payloads over stdin) — not Rust trait implementations. Grok Build: SPECULATIVE, assumed subprocess-style hooks similar to Codex. All rivals expose hooks as inter-process, not crate-internal traits."
      },
      {
        "id": "EXT-4",
        "lens": "extensions-skills-hooks",
        "title": "Slash-command registry does not load external commands or skills",
        "severity": "medium",
        "impact": 3,
        "effort": 2,
        "corrected_impact": 4,
        "corrected_effort": 4,
        "reasoning": "The claim is substantially accurate with full confirmation from the codebase:\n\n1. **Registry initialization is hardcoded to builtins only:**\n   - `/home/daniel/Projects/agents/anie/crates/anie-cli/src/commands.rs:58-61`: `with_builtins()` creates registry with `builtin_commands()` only. No constructor accepts external commands.\n   - `/home/daniel/Projects/agents/anie/crates/anie-cli/src/bootstrap.rs:109`: Registry instantiated once at startup via `CommandRegistry::with_builtins()`, stored in `ControllerState.command_registry`.\n\n2. **No public mechanism to append external commands at runtime:**\n   - `/home/daniel/Projects/agents/anie/crates/anie-cli/src/commands.rs:167-176`: The `register()` method is gated with `#[cfg(test)]`, meaning it's test-only and inaccessible at runtime (line 167).\n   - Registry is stored as immutable field in ControllerState (`/home/daniel/Projects/agents/anie/crates/anie-cli/src/controller.rs:1184`).\n   - Grep for `command_registry.register` outside tests: zero results.\n\n3. **Type system has dead code for external sources:**\n   - `/home/daniel/Projects/agents/anie/crates/anie-tui/src/commands.rs:27-36`: `SlashCommandSource` enum includes `Extension`, `Prompt`, `Skill` variants that are never constructed in production code.\n   - Comments in `/home/daniel/Projects/agents/anie/crates/anie-cli/src/commands.rs:16-17` acknowledge this: \"When extensions (plan 10) arrive, they register their commands...\"\n   - Similar comment at `/home/daniel/Projects/agents/anie/crates/anie-tui/src/commands.rs:23-25`: \"Non-`Builtin` variants are unused today — they'll be constructed once extensions (plan 10)...\"\n\n4. **Extension system does not exist:**\n   - No `anie-extensions` crate directory exists in `/home/daniel/Projects/agents/anie/crates/`.\n   - Plan 10 (`/home/daniel/Projects/agents/anie/docs/refactor_plans/10_extension_system_pi_port.md`) documents the future extension system as a multi-week, seven-phase effort still in planning.\n   - No ExtensionHost, manifest discovery, or JSON-RPC infrastructure found in codebase.\n\n5. **The registry is read-only at runtime:**\n   - Only access to registry post-initialization is `/home/daniel/Projects/agents/anie/crates/anie-cli/src/controller.rs:893` for `format_help()`.\n   - Methods available: `lookup()`, `all()`, `grouped_by_source()`, `format_help()` — all read-only.\n\nThe claim accurately describes a genuine gap: the command registry is a static, immutable builtin-only system with no runtime extension mechanism.",
        "scaffolding": "/home/daniel/Projects/agents/anie/crates/anie-cli/src/commands.rs:52-112 (CommandRegistry struct with read-only API) + /home/daniel/Projects/agents/anie/crates/anie-tui/src/commands.rs:27-49 (SlashCommandSource enum with Extension/Prompt/Skill variants) + test-gated register() method at line 167-176",
        "claim": "CommandRegistry (anie-cli/src/commands.rs:52–112) is hardcoded to builtins via with_builtins() → builtin_commands(). There is no mechanism to append Extension, Skill, or Prompt commands at runtime. The registry is constructed once at startup and never mutated. SlashCommandSource variants exist in the type system but are dead code.",
        "rival_baseline": "Claude Code (pi): ExtensionHost aggregates tool/command registrations from extensions at initialize time; registry is dynamic (extensions can register via JSON-RPC). Codex: hooks are external commands that can be configured per config.toml; config mutation adds entries. Both rivals populate registries from external sources; anie does not."
      },
      {
        "id": "EXT-5",
        "lens": "extensions-skills-hooks",
        "title": "No public surface for custom ProviderError handling or provider registration at runtime",
        "severity": "low",
        "impact": 2,
        "effort": 5,
        "corrected_impact": 3,
        "corrected_effort": 4,
        "reasoning": "The claim is confirmed. No public surface exists for runtime provider registration:\n\n1. ProviderRegistry.register() requires &mut (anie-provider/src/registry.rs:20), but the registry is wrapped in Arc<ProviderRegistry> at bootstrap (anie-cli/src/bootstrap.rs:34), making it immutable after initialization.\n\n2. ProviderError enum (anie-provider/src/error.rs:13-185) has 14 fixed variants — no custom error handling mechanism for extensions.\n\n3. Provider trait (anie-provider/src/provider.rs:32-53) is public and implementable, but only as a compile-time dependency. Test code implements MockProvider and PausingProvider in-tree, but there is no subprocess/RPC mechanism to register external providers.\n\n4. Plan 10 Phase 7 (docs/refactor_plans/10_extension_system_pi_port.md:705-753) explicitly defers ExtensionProvider wrapper pending OAuth, with rationale that subprocess-based extensions will use JSON-RPC to register providers at runtime. This is not implemented.\n\n5. No feature flags or config knobs for dynamic loading exist (verified Cargo.toml workspace).\n\n6. RPC mode (anie-cli/src/rpc.rs) handles prompts and model-select but not provider registration.\n\nThe gap is genuine. ProviderError is a closed enum and provider registration is build-time only. Runtime extensibility is a planned feature (Phase 7), not a current gap.",
        "scaffolding": "anie-provider/src/provider.rs:32-53 (Provider trait, public); anie-provider/src/registry.rs:20-22 (register method, but registry is immutable after bootstrap); docs/refactor_plans/10_extension_system_pi_port.md:726-741 (ExtensionProvider sketch in Phase 7, deferred)",
        "claim": "ProviderError taxonomy (anie-provider/src/error.rs) is comprehensive (14 variants) but provider registration is static-only. The Provider trait and provider registry are built at compile time; no mechanism exists for extensions to register new providers at runtime. Plan 10 Phase 7 (deferred) sketches ExtensionProvider wrapper but is not started.",
        "rival_baseline": "Claude Code (pi): Phase 7 of Plan 10 allows extensions to register providers via providers/register RPC, including OAuth-backed ones. Codex: Providers are baked-in (OpenAI + OSS detection); no extensibility noted. Grok Build: SPECULATIVE. Only pi supports extensible provider registration."
      },
      {
        "id": "EXT-6",
        "lens": "extensions-skills-hooks",
        "title": "No message renderer registration or custom message types supported from extensions",
        "severity": "low",
        "impact": 1,
        "effort": 4,
        "corrected_impact": 5,
        "corrected_effort": 4,
        "reasoning": "anie-protocol/src/messages.rs (lines 19-20) defines Custom variant, but there is zero support for message renderer registration or custom message rendering. anie-tui/src/app.rs (lines 1979-1982) shows Custom messages are rendered as hardcoded system messages with format \"[custom:{}] {}\", not via any extension renderer. No anie-extensions crate exists (plan 07 removed the stub; plan 10 phases 1-6 are unstarted). No WidgetNode type, no extension_widget.rs, no extension_renderer.rs, no renderer trait or registration system anywhere in the codebase. Plan 10 Phase 6 (lines 627-702) clearly describes what's needed (WidgetNode markup, registerers/register RPC, extension renderers, TUI widget placement) but is entirely unimplemented.",
        "scaffolding": "anie-protocol/src/messages.rs:19-20 (Custom variant exists but unused for extension rendering)",
        "claim": "Message type extension is limited. anie-protocol/src/messages.rs defines Message with Custom variant (lines ≤80), but there is no renderer registration system or mechanism for extensions to handle custom message rendering. Plan 10 Phase 6 (message renderers and widgets — lines 627–702) sketches this but is unstarted. The TUI has no extension-widget overlay system.",
        "rival_baseline": "Claude Code (pi): Phase 6 allows extensions to register message renderers via renderers/register RPC; extensions can paint custom WidgetNode markup above/below editor. Codex: TUI renders built-in cell types (user, assistant, exec, MCP, approval, branch, compaction); no extension renderer hook mentioned. Grok Build: SPECULATIVE. Only pi supports extension message rendering."
      },
      {
        "id": "PLAN-1",
        "lens": "agentic-planning-verify",
        "title": "No explicit plan/todo/task-list tool",
        "severity": "high",
        "impact": 4,
        "effort": 3,
        "corrected_impact": 4,
        "corrected_effort": 3,
        "reasoning": "Comprehensive search confirms anie lacks model-facing planning tools: (1) No PlanTool, TodoTool, WriteOutline, CreateTask, or similar in the tool registry — only read/write/bash/grep/find/ls/recurse/web tools registered in bootstrap.rs:143-158 and exported from anie-tools/src/lib.rs:15-23. (2) No Plan/Task/Todo/Outline variants in SessionEntry enum (anie-session/src/lib.rs), which only has Message, Compaction, ModelChange, ThinkingChange, Label. (3) No /plan, /todo, /outline, /task slash commands in builtin catalog (anie-cli/src/commands.rs:238-318). (4) CustomMessage type exists (anie-protocol/src/messages.rs:82-89) with opaque extensibility, but is never actually used for planning (verified via grep cross-reference). (5) RecurseTool (anie-tools/src/recurse.rs) enables scoped sub-agent calls for re-analysis, not explicit planning state. Planning is purely implicit via conversation context; no persistent artifact the model can write/inspect/revise. This matches the claim's core assertion: \"planning state is implicit in agent conversation context, not reified as persistent state the model can inspect/revise.\"",
        "scaffolding": "CustomMessage type in anie-protocol/src/messages.rs:82-89 provides extensibility hook; SessionEntry could support a Plan variant; Tool trait is modular and could support a PlanTool. But none of this exists yet.",
        "claim": "anie lacks a model-facing tool for explicit planning, task decomposition, or checkpoint writing (e.g., WriteOutline, WritePlan, TodoList). Planning state is implicit in agent conversation context, not reified as persistent state the model can inspect/revise.",
        "rival_baseline": "Claude Code's /plan mode, TodoWrite tool, and step-tracking via plan context let users explicitly write multi-turn plans as persistent artifacts. Verifier loop checks plan against observations. SPECULATIVE: Codex has Plan/Task types in core submission model."
      },
      {
        "id": "RECURSE-1",
        "lens": "agentic-planning-verify",
        "title": "Recursion is strictly serial, not parallel fan-out",
        "severity": "high",
        "impact": 4,
        "effort": 4,
        "corrected_impact": 4,
        "corrected_effort": 4,
        "reasoning": "The recurse tool in crates/anie-tools/src/recurse.rs:215-223 drives a single sub-agent serially via `while !machine.is_finished() { machine.next_step(...).await }`. No tokio::spawn, join_all, or similar mechanism exists to spawn N independent sub-agents in parallel. The RwLock on ControllerContextProvider (recurse_provider.rs:70) supports concurrent **reads** of the context store during parallel recurse invocations, but each recurse call itself blocks the model until its sub-agent completes. The fork_to_child_session mechanism (anie-session/src/lib.rs:613) exists but is a user-facing session-branching feature, not an automatic task-decomposition system. ToolExecutionMode::Parallel (agent_loop.rs:1400-1404) parallelizes tool calls within a single agent, not sub-agent spawning. The RLM design doc (docs/rlm_2026-04-29/02_recurse_tool.md) explicitly specifies shape 1 as a serial per-call sub-agent invocation with a shared budget, not parallel decomposition.",
        "scaffolding": "crates/anie-session/src/lib.rs:613 (fork_to_child_session), crates/anie-agent/src/agent_loop.rs:1400-1404 (ToolExecutionMode::Parallel for tool parallelism only), crates/anie-cli/src/recurse_provider.rs:67-70 (RwLock for concurrent reads, not sub-agent parallelism)",
        "claim": "recurse tool invokes one sub-agent at a time, waiting for completion before returning (anie-tools/src/recurse.rs:218-220: `while !machine.is_finished() { machine.next_step(...).await }`). No mechanism to spawn multiple independent sub-agents in parallel for task decomposition.",
        "rival_baseline": "Claude Code's sub-agent swarming dispatches 5-8 independent agents (each with ~167K context tokens) to parallel worktrees or forks, with aggregation. SPECULATIVE: Codex/pi may support spawn(N) with N concurrent sub-session branches."
      },
      {
        "id": "VERIFY-1",
        "lens": "agentic-planning-verify",
        "title": "No verifier/self-critique loop consumer of BeforeModelPolicy seam",
        "severity": "medium",
        "impact": 3,
        "effort": 3,
        "corrected_impact": 4,
        "corrected_effort": 4,
        "reasoning": "The claim is substantially correct. BeforeModelPolicy is defined in /home/daniel/Projects/agents/anie/crates/anie-agent/src/agent_loop.rs:342-346 with three viable response types (Continue, AppendMessages, ReplaceMessages). Currently only two implementations exist in production code: (1) NoopBeforeModelPolicy (agent_loop.rs:350-357) and (2) ContextVirtualizationPolicy (context_virt.rs:468-560 in the BeforeModelPolicy impl block). The test file has two additional test-only implementations (AppendOncePolicy, ReplaceOncePolicy in agent_loop_policy.rs) but these don't count as real consumers. ContextVirtualizationPolicy handles context virtualization and message eviction/archival - it is NOT a verifier/critique tool. It deals with context ceiling enforcement and relevance ranking, not step outcome validation. There is zero implementation of any verifier, self-critique, or checkpoint policy that validates step outcomes, re-routes on failure, or updates plans based on step completion status. The agent_loop.rs code explicitly documents (lines 336-338): \"Future hooks (after-model, on-tool-error, before-tool, after-stream-error) get their own traits when real consumers materialize\" — verifier/critique hooks do not yet exist. The docs/small_model_capability_ideas_2026-04-29.md sketches a conceptual \"Verifier\" and \"Critic\" intent (sections 6-8, lines 220-290+) but these are unimplemented ideas, not wired code. No AgentIntent variant exists for Critic or VerifierPolicy-driven behavior. The AgentDecision enum and AgentIntent enum (agent_loop.rs ~line 800+) contain only: ModelTurn, ExecuteTools, AppendFollowUps, RunCompactionGate, Finish — no verification/critique step. The claimed rival baseline (Claude Code's verifier loop checking each step against plan) does not have a corresponding implementation in anie yet.",
        "scaffolding": "BeforeModelPolicy trait definition + execution hook (agent_loop.rs:342-346, 800-850 approx. for call site), plus context-virt.rs implements eviction/archival messaging but NOT step outcome validation. AgentRunMachine REPL Read/Eval/Print/Decide architecture exists (agent_loop.rs) but Decide only routes among {ModelTurn, ExecuteTools, AppendFollowUps, RunCompactionGate, Finish} — no decision logic for verifying/critiquing outcomes.",
        "claim": "BeforeModelPolicy (agent_loop.rs:342-346) exists as a before-model hook but only ContextVirtualizationPolicy is wired (controller.rs:1816, context_virt.rs:468). No verifier tool or loop uses BeforeModelPolicy to check, critique, or re-plan based on observations. Trait supports message injection but no consumer validates step outcomes or re-routes on failure.",
        "rival_baseline": "Claude Code's verifier loop consumer of BeforeModelPolicy would check each step against plan and either approve, ask for re-work, or update plan. Step status (completed/blocked/pending) shapes next turn's context. SPECULATIVE: pi may have explicit check/verify hooks in submission dispatch."
      },
      {
        "id": "DECOMP-1",
        "lens": "agentic-planning-verify",
        "title": "No task decomposition tool; manual prompt engineering required",
        "severity": "medium",
        "impact": 3,
        "effort": 4,
        "corrected_impact": 4,
        "corrected_effort": 4,
        "reasoning": "The claim is confirmed as a genuine gap. Investigation of the anie codebase revealed:\n\nTOOLS INVENTORY (crates/anie-tools/src/lib.rs:15-23): Exactly 8 tools exported: BashTool, EditTool, FindTool, GrepTool, LsTool, ReadTool, RecurseTool, WriteTool. Web tools (crates/anie-tools-web/src/lib.rs:74-77) add only WebReadTool and WebSearchTool. **No decompose, task, or plan tool exists.**\n\nPROTOCOL TYPES (crates/anie-protocol/src/messages.rs, events.rs, tools.rs): Protocol has only four message types: UserMessage, AssistantMessage, ToolResultMessage, CustomMessage. No Task, Subtask, Milestone, or Plan variants exist in the message hierarchy. AgentEvent enum has 20 variants (AgentStart, TurnStart, ToolExecStart, etc.) but none for task completion, subtask status, or milestone tracking.\n\nCONTEXT MANAGEMENT: BeforeModelPolicy (crates/anie-agent/src/agent_loop.rs:342-357) supports BeforeModelResponse::AppendMessages and BeforeModelResponse::ReplaceMessages, allowing message injection but no structured task ledger. External context store (crates/anie-cli/src/external_context.rs) tracks MessageId and embeddings for reranking, not task status or dependencies. RLM ledger tracks tool calls, not tasks.\n\nCOMPACTION HINT: System prompt at crates/anie-cli/src/compaction.rs:80 explicitly asks the model to preserve \"goals, progress, key decisions, file paths, and remaining tasks\" — but these are reconstructed from natural language, not from a structured task registry the agent can reference or update.\n\nRECURSION LIMITS CONFIRMED: Recursion budget is hardcoded to RECURSION_BUDGET_DEFAULT = 8 (crates/anie-cli/src/controller.rs:108), max depth is RECURSION_MAX_DEPTH = 2 (crates/anie-cli/src/controller.rs:117), and sub-agents are currently tool-free (crates/anie-cli/src/recurse_factory.rs:67-72) — no parallel spawning, SERIAL only.\n\nVERDICT: The claim accurately identifies a missing capability. anie has no structured task decomposition tool. Task decomposition, if needed, must happen via natural language in the conversation — the model receives a prompt asking it to decompose, and must track status via its own conversation text.\n\nNo scaffolding for this feature exists.",
        "scaffolding": "none",
        "claim": "Agent must ask the model to decompose tasks via conversation text. No structured decompose-task tool that outputs {name, description, dependencies, steps} subtasks the model can reference, track status, or roll up completion.",
        "rival_baseline": "Claude Code's plan mode and /decompose skill let users structure goals as {task: id, status, subtasks: []} DAG with topological ordering. Verifier can route work based on dependencies. SPECULATIVE: pi may have Task/Milestone protocol types."
      },
      {
        "id": "STEP-CHECK-1",
        "lens": "agentic-planning-verify",
        "title": "No in-turn checkpoint/validation tool to mark & verify steps",
        "severity": "low",
        "impact": 2,
        "effort": 3,
        "corrected_impact": 4,
        "corrected_effort": 3,
        "reasoning": "AgentRunMachine has explicit step phases (Read/Eval/Print/Decide, agent_loop.rs:749) with 5 intents (agent_loop.rs:644-658: ModelTurn, ExecuteTools, AppendFollowUps, RunCompactionGate, Finish) — NO CheckpointIntent or ValidateStepIntent. Tool registry (bootstrap.rs:131-186) registers 8 tools (read, write, edit, bash, grep, find, ls, recurse) — NO checkpoint or validation tool. BeforeModelPolicy (agent_loop.rs:342-357) returns Continue/AppendMessages/ReplaceMessages — NO step validation status. AfterToolCallHook (hooks.rs:49-59) overrides tool results only — NO step completion signal. AgentEvent enum (events.rs:30-121) has no checkpoint/validation/completion events. No plan documentation for this feature exists. The agent loop has the step structure but lacks any mechanism for agents to checkpoint progress, validate outcomes, or emit structured completion signals for a verifier to consume.",
        "scaffolding": "none",
        "claim": "Agent loop has explicit step phases (Read/Eval/Print/Decide in AgentRunMachine, agent_loop.rs:657-750) but no tool for agent to checkpoint/mark progress, validate step outcomes, or emit structured completion signals for the verifier to consume.",
        "rival_baseline": "Claude Code's verifier loop checks step outputs and emits {step_id, status: completed|blocked|needs_revision} to shape next model context. Checkpoint tool marks milestones so user can query progress mid-run. SPECULATIVE: Codex may have Step/Checkpoint events in submission model."
      },
      {
        "id": "EDIT-1",
        "lens": "edit-reliability",
        "title": "No unified-diff / patch-apply tool; only exact-text edit",
        "severity": "medium",
        "impact": 3,
        "effort": 4,
        "corrected_impact": 4,
        "corrected_effort": 3,
        "reasoning": "anie's EditTool (crates/anie-tools/src/edit.rs) supports only exact-text matching with fuzzy whitespace fallback (line 258 find_all_occurrences, lines 275-299 fuzzy fallback). The tool does NOT:\n1. Parse unified-diff format or context lines\n2. Understand hunk semantics\n3. Match based on context markers (\"@@\")\n4. Perform context-aware replacements like Codex's apply_patch\n\nVerified the gap:\n- No PatchTool, apply_patch, patch_parse, or unified_diff_apply in crates/anie-tools/src/ (lib.rs exports only: EditTool, WriteTool, ReadTool, BashTool, FindTool, GrepTool, LsTool, RecurseTool)\n- No \"patch\" or \"apply_patch\" string literals registered in tool names (grep found none)\n- Codex DOES have apply_patch (docs/arch/codex_summary.md:136-150) supporting \"inline unified-diff-like hunks with - / + / context lines\" and \"similar::TextDiff to locate context and compute replacements\"\n\nThis is a genuine architectural difference: anie requires exact oldText synthesis; Codex parses diffs and matches hunks flexibly with context. The gap is real and impacts usability when applying pre-made patches or when surrounding context changes.",
        "scaffolding": "crates/anie-tools/src/edit.rs uses `similar::TextDiff` (line 4, 461) for diff rendering only, not for hunk matching. No existing patch parsing infrastructure.",
        "claim": "anie has no tool to apply unified-diff format patches or context-based hunk matching. The edit tool requires exact oldText matches (with fuzzy whitespace fallback). Codex and pi both support heredoc-delimited patches with context lines and hunk merging.",
        "rival_baseline": "Codex: apply_patch tool parses unified-diff-like hunks with context lines, uses similar::TextDiff to match context and apply hunks. pi: `str_replace` tool with exact match (same as anie) but context lines are supported implicitly in the oldText. Claude Code: str_replace tool requires exact match of oldText."
      },
      {
        "id": "EDIT-2",
        "lens": "edit-reliability",
        "title": "Non-atomic multi-file edits; no transactional batch across files",
        "severity": "medium",
        "impact": 2,
        "effort": 5,
        "corrected_impact": 3,
        "corrected_effort": 3,
        "reasoning": "anie's edit tool operates strictly on one file per call (crates/anie-tools/src/edit.rs:58 explicitly states 'Edit a single file'). FileMutationQueue (crates/anie-tools/src/file_mutation_queue.rs:31-44) locks per-path only using DashMap<PathBuf, Arc<Mutex<()>>>. Tool execution (crates/anie-agent/src/agent_loop.rs:1382-1407) runs calls sequentially or in parallel, but without cross-file transaction semantics — a crash mid-batch leaves some files edited and others unchanged. No batch-edit or apply-patch tool exists (anie-tools/src/lib.rs exports only single-file tools). Tests in crates/anie-tools/src/tests.rs show only per-file locking verification, no multi-file atomicity tests. Codex comparison (docs/arch/codex_summary.md:136-150) shows Codex implements apply_patch with '*** Add file', '*** Delete file', '*** Update file', '*** Rename file' in a single atomic patch — anie has no equivalent.",
        "scaffolding": "crates/anie-tools/src/file_mutation_queue.rs (per-file locking), crates/anie-agent/src/agent_loop.rs:1382-1407 (tool execution dispatch), crates/anie-tools/src/edit.rs and write.rs (both use FileMutationQueue pattern)",
        "claim": "anie's edit tool operates on one file per call. Sequential tool calls to different files are not transactional — a crash or cancellation mid-batch leaves some files edited and others not. FileMutationQueue locks only per-file, not across files.",
        "rival_baseline": "SPECULATIVE: Codex's apply_patch format (docs/arch/codex_summary.md:142-144) supports '*** Add file', '*** Delete file', '*** Update file', '*** Rename file' in a single patch — implying atomic multi-file application within one tool call. Claude Code / pi: likely same single-file-per-call pattern as anie."
      },
      {
        "id": "EDIT-4",
        "lens": "edit-reliability",
        "title": "No rollback/undo on partial-batch failure within single file",
        "severity": "low",
        "impact": 1,
        "effort": 2,
        "corrected_impact": 2,
        "corrected_effort": 4,
        "reasoning": "The claim is accurate. Code inspection shows:\n\n1. apply_edits (crates/anie-tools/src/edit.rs:241-320) validates ALL edits sequentially (lines 251-300) with early returns on any validation failure (lines 253-255, 260-263, 282-285, 287-291).\n\n2. Post-validation checks for overlaps (lines 302-312) only occur if all edits passed individual validation.\n\n3. Application phase (lines 314-319) only executes if ALL validation passed.\n\n4. No partial-application path exists: if any edit fails validation, apply_edits returns an Err immediately via the ? operator at line 119 in execute(), preventing any writes.\n\n5. Error responses are unstructured strings (no information about which edits failed vs. which succeeded).\n\nEvidence: (a) Lines 251-300 show sequential validation with multiple early-exit points; (b) Line 302 and subsequent overlap checks only happen after the validation loop completes successfully; (c) Line 315-317 applies all matched edits or none; (d) execute() line 119 uses ? to propagate failures without fallback; (e) No tests (tests.rs:1034-1398) exercise partial-success scenarios; (f) No error structs carry per-edit failure info.\n\nThis is a genuine gap, though mitigated in practice by fuzzy matching (lines 275-299 handle whitespace/indentation variations before failing), which catches most real-world edit alignment issues.",
        "scaffolding": "none",
        "claim": "Within a single edit call to one file, if one of the 100 edits fails validation (e.g., oldText mismatch after fuzzy fallback), all prior edits in that batch are discarded and the original file is left unchanged. There is no way to apply subset and report which edits failed.",
        "rival_baseline": "SPECULATIVE: Codex apply_patch likely has same all-or-nothing semantics per patch. Claude Code str_replace: likely same all-or-nothing per tool call."
      },
      {
        "id": "EDIT-5",
        "lens": "edit-reliability",
        "title": "No structured patch format for LLM generation; requires exact string matching",
        "severity": "high",
        "impact": 3,
        "effort": 4,
        "corrected_impact": 4,
        "corrected_effort": 5,
        "reasoning": "The claim is confirmed. anie's edit tool (crates/anie-tools/src/edit.rs) requires exact text matching via oldText/newText parameters (ToolDef lines 59-80, definition line 58). The fuzzy fallback (lines 329-355, 423-453) only normalizes whitespace (collapsing runs to single spaces) but provides no context-line matching, line-number anchoring, or hunk-based matching like Codex's unified-diff format. The ToolDef JSON schema has exactly two properties per edit: oldText and newText, with no contextLines, lineNumber, or hunk fields. This is a genuine structural gap vs. Codex (which supports 3-line context hunks) and represents a real brittleness: if surrounding code changes slightly, the LLM's oldText becomes stale even if semantically similar. pi's edit tool is also exact-only (pi_summary.md:179), so this is not a pi-adoption gap, but it is a real competitive gap vs. Codex. The claim's cited evidence (line 58 description, lines 59-80 parameters, lines 455-458 fuzzy logic, no AST-based anchoring) is all accurate.",
        "scaffolding": "none",
        "claim": "anie's edit tool requires the LLM to produce exact-string oldText values, with no way to pass context lines or structural hints (AST-based anchors, line numbers, semantic markers). The fuzzy fallback masks whitespace typos but cannot recover from semantic drift in surrounding code.",
        "rival_baseline": "Codex: apply_patch format (docs/arch/codex_summary.md:143) uses unified-diff hunks with 3-line context, allowing hunk matching even if code moved slightly. pi: str_replace also exact-only (similar to anie). Claude Code: exact-only (str_replace model)."
      },
      {
        "id": "SESSION-1",
        "lens": "session-ux",
        "title": "Session picker overlay is a stub — no interactive session browser",
        "severity": "medium",
        "impact": 2,
        "effort": 3,
        "corrected_impact": 2,
        "corrected_effort": 3,
        "reasoning": "The session picker overlay is genuinely a non-functional stub with zero interactive capability. File: crates/anie-tui/src/overlays/session_picker.rs (91 LOC total). The implementation is minimal scaffolding: SessionPickerScreen struct with three trait methods (dispatch_key, dispatch_tick, dispatch_render). Key evidence: (1) Line 1-4: Explicitly documented as \"placeholder\" with comment \"Not yet implemented — renders a stub and dismisses on any key\"; (2) Line 39: dispatch_render unconditionally calls render_placeholder_panel (the shared stub helper) with hardcoded text; (3) Lines 30-31: dispatch_key returns OverlayOutcome::Dismiss on any input, dismissing immediately; (4) Lines 34-35: dispatch_tick returns OverlayOutcome::Idle (no-op); (5) Zero stateful session data: no field for sessions list, no cursor index, no filter state, no search field—contrast with ModelPickerPane (model_picker.rs:29+) which has `models: Vec<ModelInfo>`, `filtered_indices: Vec<usize>`, `selected: usize`, `scroll: usize`, `search: SearchField`; (6) No UI logic: no key handling for navigation (Up/Down), no selection logic, no rendering of session list or cursor; (7) Never wired: grepping shows SessionPickerScreen is defined but never instantiated; no open_session_picker() function exists in app.rs (unlike open_model_picker_for_current_provider at line 1522); (8) Fallback exists: /session list prints formatted text via controller.rs:845-849; /session <id> allows switching by known ID via lines 850-877. The stub was deliberately created as part of plan 02 phase 6 sub-step B (documented in docs/completed/refactor_plans/fixes/02a_overlay_placeholder_stubs.md) precisely as a \"compilable placeholder\" to provide a landing pad for future implementation. All design decisions mentioned in the claim are architecturally sound and present.",
        "scaffolding": "crates/anie-tui/src/overlays/session_picker.rs:1-41 (stub implementation); crates/anie-tui/src/overlay.rs:29-35 (OverlayOutcome trait); crates/anie-cli/src/controller.rs:845-849 (command-line list fallback)",
        "claim": "Users cannot browse and select sessions from a visual picker; /session list|<id> is the only command-line interface.",
        "rival_baseline": "Claude Code / pi has session persistence with branch tracking via parent_id; Codex tracks threads in SQLite (state/state.db) with UI picker / browser for thread selection. Claude Code allows /session <id> switching but has no explicit visual picker noted in docs."
      },
      {
        "id": "SESSION-3",
        "lens": "session-ux",
        "title": "/session command lacks checkpoint/rewind subcommands — no mid-session undo",
        "severity": "low",
        "impact": 2,
        "effort": 3,
        "corrected_impact": 4,
        "corrected_effort": 3,
        "reasoning": "The claim is CONFIRMED. Codebase inspection shows:\n\n1. **SESSION_SUBCOMMANDS** (crates/anie-cli/src/commands.rs:29) = [\"list\"] only. No checkpoint/rewind/branch-list variants.\n\n2. **/session command handler** (crates/anie-tui/src/app.rs) matches: None->GetState, \"list\"->ListSessions, or <id>->SwitchSession. No other subcommands.\n\n3. **UiAction variants** (crates/anie-tui/src/app.rs) include ListSessions, SwitchSession, ForkSession, NewSession, but NO checkpoint/rewind/navigate-branch actions.\n\n4. **Underlying infrastructure partially exists but NOT exposed**:\n   - SessionEntry::Compaction type exists (crates/anie-session/src/lib.rs:135-154) but is internal infrastructure, not user-facing\n   - SessionManager::fork(from_entry_id) method exists (crates/anie-session/src/lib.rs:604-610) — can fork from any entry\n   - SessionHandle::fork() (crates/anie-cli/src/runtime/session_handle.rs:123-128) only exposes fork from current leaf (via fork_to_child_session), NOT from arbitrary entry\n\n5. **Planning confirms absence**: docs/notes/commands_and_slash_menu.md explicitly lists \"/tree: Navigate session history tree\" as \"Not implemented\" (Status column).\n\n**What users CAN do**: `/fork` (branch from now), `/session list`, `/session <id>` (switch between sessions).\n**What users CANNOT do**: Rewind/checkpoint within a session, name checkpoints, list branch history, navigate to specific turns, create a branch from a past turn mid-session.\n\nThe gap is real: forking infrastructure exists but is not exposed via CLI commands for mid-session navigation.",
        "scaffolding": "crates/anie-session/src/lib.rs:604 (SessionManager::fork method with from_entry_id parameter)",
        "claim": "Users cannot rewind the session to an earlier point, create named checkpoints, or list/navigate branch history within a session. /session only supports list|<id> for switching sessions.",
        "rival_baseline": "Claude Code (pi comparison) does not explicitly document mid-session rewind; Codex docs note thread resumption but not mid-thread checkpoints. However, both support forking from any point via parent_id tracking. Checkpoint/rewind is not clearly documented for either rival."
      },
      {
        "id": "SESSION-4",
        "lens": "session-ux",
        "title": "/fork creates child sessions but lacks automatic branch summarization",
        "severity": "low",
        "impact": 1,
        "effort": 3,
        "corrected_impact": 3,
        "corrected_effort": 4,
        "reasoning": "Fork mechanism is fully implemented (crates/anie-session/src/lib.rs:613-629, crates/anie-cli/src/controller.rs:1557-1565) and working correctly - it clones the active branch into a child session. However, the claim about missing branch summarization is completely verified. There is no BranchSummary or BranchSummaryEntry variant in the SessionEntry enum (which currently has only: Message, Compaction, ModelChange, ThinkingChange, Label at lines 126-187). No code path creates any entry when fork occurs to summarize the abandoned branch or record its file operations. The comparison doc explicitly confirms this gap (anie_vs_pi_comparison.md:390-391): \"anie has fork and fork_to_child_session but no automatic branch summarization.\" Pi's equivalent (branchWithSummary and branch-summarization.ts) is completely absent from anie. While CompactionDetails exists for tracking file ops (crates/anie-session/src/lib.rs:257-267), it is never instantiated during fork - only during mid-turn compaction. This is a genuine functional gap, not scaffolding.",
        "scaffolding": "crates/anie-session/src/lib.rs:257-267 (CompactionDetails struct for file-op tracking, but unused at fork time)",
        "claim": "Forking is implemented and working (fork_to_child_session copies active branch), but does not automatically summarize the abandoned branch path or record file operations on the forked-from branch.",
        "rival_baseline": "pi (Claude Code) has branchWithSummary(branchFromId, summary) and automatic branch summarization via branch-summarization.ts; tracks file_operations in summaries. Codex threads are immutable (new thread copies path); no explicit branch summarization noted."
      },
      {
        "id": "SESSION-5",
        "lens": "session-ux",
        "title": "No working tree snapshot/restore — git-level undo not exposed",
        "severity": "low",
        "impact": 1,
        "effort": 4,
        "corrected_impact": 4,
        "corrected_effort": 4,
        "reasoning": "Exhaustive grep and code review confirms anie has no working tree snapshot/restore mechanism. Evidence: (1) Zero matches for git reset/checkout/restore/revert in entire codebase. (2) `/diff` command at crates/anie-cli/src/runtime/session_handle.rs:130-188 is read-only, renders metadata only. (3) UiAction enum (crates/anie-tui/src/app.rs) contains 30+ variants but none for restore/revert/undo. (4) Builtin command registry (crates/anie-cli/src/commands.rs:238-318) lists 21 commands - no restore/revert/snapshot. (5) Session persistence (crates/anie-session/src/lib.rs) tracks CompactionDetails with file paths only, not snapshots. (6) Registered tools (crates/anie-cli/src/bootstrap.rs:131-186) are Read/Write/Edit/Bash/Grep/Find/Ls/Web - no git or working tree tools. Sessions branch via `/fork` but don't revert file state. File mutations are logged as metadata for resume hints, not reversible snapshots. The claim accurately identifies a genuine absent feature: no mechanism to snapshot or restore working directory state to an earlier session checkpoint.",
        "scaffolding": "none",
        "claim": "Sessions track messages and compaction but do not expose a mechanism to snapshot or restore the working directory state (file edits made by tool calls) to an earlier point. Users cannot 'rewind' file changes to a prior session checkpoint.",
        "rival_baseline": "Codex snapshot-mode and exec-server support task isolation via sandboxing but do not expose explicit tree restore. Claude Code (pi) has no documented tree-level checkpoint/restore. Typical pattern in Codex: revert via shell command or git reset (model-driven, not framework-driven)."
      },
      {
        "id": "COST-1",
        "lens": "provider-breadth-cost",
        "title": "Cost struct is defined but never populated or enforced at runtime",
        "severity": "medium",
        "impact": 3,
        "effort": 2,
        "corrected_impact": 4,
        "corrected_effort": 3,
        "reasoning": "The claim is confirmed as a genuine gap. Evidence:\n\n1. **Cost struct is defined** with fields for input, output, cache_read, cache_write, and total in /home/daniel/Projects/agents/anie/crates/anie-protocol/src/usage.rs:24-35, derived with Default trait so all fields initialize to 0.0.\n\n2. **Cost is never populated during streaming**: \n   - In /home/daniel/Projects/agents/anie/crates/anie-providers-builtin/src/anthropic.rs:762-779, the `update_usage()` function populates token counts but does NOT populate the Cost struct. \n   - In /home/daniel/Projects/agents/anie/crates/anie-providers-builtin/src/anthropic.rs:704-722, `into_message()` returns AssistantMessage with `usage: std::mem::take(&mut self.usage)` where usage.cost remains at default (all zeros).\n   - In /home/daniel/Projects/agents/anie/crates/anie-providers-builtin/src/openai/streaming.rs:286-347, OpenAI's `into_message()` similarly returns usage without populating cost (line 335).\n   - Similar pattern in all other provider implementations (no cost calculation found anywhere via `grep -rn \"cost\\.total|cost\\.input\"` returns no results).\n\n3. **CostPerMillion exists in Model but is never used for calculation**: \n   - Model has `cost_per_million: CostPerMillion` field (/home/daniel/Projects/agents/anie/crates/anie-provider/src/model.rs:326).\n   - Model catalog populates real prices like input: 3.0, output: 15.0 for Claude Sonnet in /home/daniel/Projects/agents/anie/crates/anie-providers-builtin/src/models.rs:39-44.\n   - But `grep -rn \"model\\.cost_per_million\"` returns no actual usage locations, only test assertions.\n   - No code multiplies `usage.input_tokens * model.cost_per_million.input / 1_000_000` anywhere.\n\n4. **No per-session or per-run cost ceiling enforcement**:\n   - `grep -rn \"max_cost|cost.*limit|cost.*ceil\"` returns zero results.\n   - Usage data is never displayed to the user in logs or UI.\n   - No RetryPolicy variant or ProviderError variant for cost-limit exceeded.\n\nThe gap is genuinely absent: Cost struct is scaffolding without implementation.",
        "scaffolding": "cost_per_million field on Model with actual catalog values (models.rs:39-44, etc.), Cost struct definition with Default impl, Usage.cost field that defaults to Cost::default() with all zeros.",
        "claim": "Usage.cost and Cost fields exist (crates/anie-protocol/src/usage.rs) but are never filled during streaming; no code multiplies usage tokens by model.cost_per_million to compute actual cost, and no per-session or per-run cost ceiling is enforced.",
        "rival_baseline": "pi's AssistantMessage.usage includes cost (usage.cost per pi_summary.md:77). SPECULATIVE: pi likely calculates cost = usage tokens × model.cost structure; no evidence found of per-session cost ceiling in pi's code either, but pi's streaming reports cost inline."
      },
      {
        "id": "COST-2",
        "lens": "provider-breadth-cost",
        "title": "No per-run or per-session cost budget enforcement",
        "severity": "medium",
        "impact": 3,
        "effort": 4,
        "corrected_impact": 5,
        "corrected_effort": 3,
        "reasoning": "Code audit confirms the gap is genuine. anie has wire-compatible Cost structures in anie-protocol/src/usage.rs (Cost struct with input/output/cache_read/cache_write/total fields, integrated into Usage) but they are never populated by providers and never used for enforcement. No configuration fields for cost budgets exist in CompactionConfig (anie-config/src/lib.rs:327-365) or ContextConfig (lib.rs:380-387). RetryPolicy::decide (anie-cli/src/retry_policy.rs:110-240) has no cost-based decision logic. SessionManager and controller track only token and compaction budgets, not monetary cost. Session schema v4 (anie-session/src/lib.rs:69-90) contains no cost tracking fields. Verified: no max_spend, cost_ceiling, budget_dollar, max_cost, or cost_limit exist in any crate. The Cost struct is purely a data carrier for future use; cost_per_million metadata on models is never applied to tokens to compute actual spend.",
        "scaffolding": "Cost struct defined in anie-protocol/src/usage.rs:24-35; CostPerMillion metadata storage in anie-provider/src/model.rs:7-23 and carried on Model:326. Both never used for runtime enforcement or accumulation.",
        "claim": "anie has no configuration option, runtime check, or policy to limit cumulative spend per session, per run, or per user. Token budgets exist (compaction reserve, recursion depth budget, tool output truncation), but no cost budgets.",
        "rival_baseline": "SPECULATIVE: pi's compat model (docs/anie_vs_pi_comparison.md:165) mentions OpenRouter's max_price field but it's unclear if pi enforces it at runtime. Neither pi nor anie appears to have shipping cost-ceiling enforcement; both assume user manages their API spend at the provider level."
      },
      {
        "id": "EVAL-1",
        "lens": "eval-observability",
        "title": "No eval-harness crate or scenario runner implemented",
        "severity": "critical",
        "impact": 5,
        "effort": 4,
        "corrected_impact": 4,
        "corrected_effort": 4,
        "reasoning": "Plan 07 (docs/rlm_2026-04-29/07_evaluation_harness.md) specifies 7 implementation phases totaling ~4 weeks. Phase 1 (HarnessMode enum + --harness-mode flag) was completed on 2026-04-29 (commits 8a162d3 through 525a0c5). Phases 2-7 (scenario TOML format, eval runner binary, scenario corpus, automated checks, rubric scoring, multi-run reporting) were never executed. Codebase contains no anie-evals crate (13 workspace members total, not 15), no Scenario/Score/RunResult types, no rubric trait, no eval binary, no scenario TOML files, and zero commits to eval infrastructure after the plan document. The integration-tests crate (crates/anie-integration-tests/) contains only mock provider scaffolding and agent loop helpers, not scenario-based evaluation harness. The harness-mode feature functions correctly for measurement isolation (baseline/current/rlm modes control tool registration and compaction gate), but cannot be used to aggregate results across scenarios or compare models because the scenario runner, scoring framework, and reporting infrastructure do not exist.",
        "scaffolding": "crates/anie-cli/src/harness_mode.rs:29-73 (HarnessMode enum with 3 variants), crates/anie-cli/src/lib.rs:~line 98 (--harness-mode flag definition), docs/rlm_2026-04-29/07_evaluation_harness.md (complete specification document)",
        "claim": "Plan 07 (docs/rlm_2026-04-29/07_evaluation_harness.md) specifies a complete `anie-evals` crate with scenario TOML files, automated/rubric scoring, and multi-run aggregation. This does not exist in code—only the plan document.",
        "rival_baseline": "Claude Code (Codex) likely has internal eval harnesses that measure model quality on realistic agent tasks. Grok Build (if it exists) would need similar infrastructure to claim rival status. Without this, 'rival to Codex' is unverifiable—there is no standard way to measure and compare across models/harness-modes."
      },
      {
        "id": "EVAL-3",
        "lens": "eval-observability",
        "title": "Tool success/failure rate not tracked; tool reliability opaque",
        "severity": "high",
        "impact": 3,
        "effort": 2,
        "corrected_impact": 3,
        "corrected_effort": 4,
        "reasoning": "Verified that AgentEvent::ToolExecEnd (anie-protocol/src/events.rs:59-64) captures individual tool outcome data with is_error flag and timing. However, there is NO aggregating structure for tool metrics anywhere in the codebase. CompactionStatsAtomic (anie-cli/src/compaction_stats.rs) exists but only tracks compaction phases, not tool outcomes. The /state endpoint summary (controller.rs:2175) formats compaction_stats only, not tool stats. No ToolStatsAtomic, ToolMetrics, or tool outcome ledger exists. The TUI (anie-tui/src/app.rs:873) captures ToolExecEnd events for display but does not aggregate them. Therefore, statements like \"read tool succeeds 98.5% of the time\" cannot be verified from live runs, confirming the gap is genuine.",
        "scaffolding": "anie-protocol/src/events.rs:59-64 (AgentEvent::ToolExecEnd with is_error), anie-cli/src/compaction_stats.rs (CompactionStatsAtomic pattern for phase tracking)",
        "claim": "AgentEvent::ToolExecEnd captures result + is_error flag. However, there is no aggregating counter for (tool_name → success_count, failure_count, avg_duration). Without this, statements like 'read tool succeeds 98.5% of the time' or 'grep is 3x slower than find' are unverifiable from live runs.",
        "rival_baseline": "SPECULATIVE: Rival agent harnesses would track: tool success rate per tool, median latency, error types (timeout, permission, parse), and flag slow/flaky tools. This data guides optimizer decisions (prefer find over grep for large repos) and alerts on regressions."
      },
      {
        "id": "EVAL-5",
        "lens": "eval-observability",
        "title": "No benchmark corpus for real agent tasks (coding, refactoring, debugging, docs)",
        "severity": "high",
        "impact": 5,
        "effort": 4,
        "corrected_impact": 4,
        "corrected_effort": 5,
        "reasoning": "The claim is accurate: there is NO benchmark corpus or evaluation harness for real agent tasks in anie. The codebase contains ONLY the foundational `--harness-mode` flag (implemented in crates/anie-cli/src/harness_mode.rs with three modes: baseline, current, rlm). The detailed plan document exists at docs/rlm_2026-04-29/07_evaluation_harness.md (390 lines), but implementation has not begun. The missing components are: (1) anie-evals crate (not in Cargo.toml members list), (2) scenario corpus (zero .toml files in scenarios/ directories), (3) Scenario/CheckResult/AutomatedCheck types (grep finds no implementations), (4) eval runner binary (no evals.rs found), (5) rubric-based scoring with LLM-as-judge (no code), (6) results storage/reporting infrastructure (no results/ artifacts). The 5 integration test files (agent_session.rs, agent_tui.rs, config_wiring.rs, provider_replay.rs, session_resume.rs) measure harness infrastructure correctness (tool dispatch, TUI rendering, session persistence) but not end-to-end agent quality on realistic coding/refactoring/debugging tasks. Plan status line: \"ready to spec; ships in parallel with Phase A of Plan 06.\"",
        "scaffolding": "crates/anie-cli/src/harness_mode.rs (3 modes defined, 13 tests), crates/anie-integration-tests/tests/ (5 test files measuring infrastructure, not agent quality), docs/rlm_2026-04-29/07_evaluation_harness.md (complete 390-line plan with scenario format, scoring, reproducibility, exit criteria)",
        "claim": "benchmarks_and_evaluation.md (line 15–29) specifies an internal benchmark suite with 4 scoring dimensions (completion, correctness, efficiency, time). Plan 07 specifies 30–40 scenarios across 5 families (long_context, repo_navigation, editing, tool_use, cancellation). Neither the corpus nor the runner exists.",
        "rival_baseline": "Claude Code's internal test suite likely includes: 'fix a bug in a real codebase given a PR description', 'refactor a 500-line function', 'add a feature end-to-end', 'debug a flaky test'. These measure true agent capability (tool planning, error recovery, verification loops), not just unit behavior."
      },
      {
        "id": "EVAL-6",
        "lens": "eval-observability",
        "title": "Compaction telemetry only per-session; no fleet-wide observability",
        "severity": "medium",
        "impact": 2,
        "effort": 2,
        "corrected_impact": 4,
        "corrected_effort": 4,
        "reasoning": "CompactionStatsAtomic in crates/anie-cli/src/compaction_stats.rs (lines 27-72) is a per-session in-process counter with no durability, export, or fleet aggregation. Code explicitly documents \"this-process-lifetime, not durable\" (crates/anie-cli/src/controller.rs:2250-2257). The telemetry plan (docs/midturn_compaction_2026-04-27/06_compaction_telemetry.md) explicitly defers \"Persistent metrics across sessions\" as a future plan (lines 245-246). No metrics database, export pipeline, or analysis tools exist. Compaction stats visible only in live /state command within current session; counters reset on session switch. PR B exit criteria (lines 178-181) only require /state visibility, not persistence or export.",
        "scaffolding": "crates/anie-cli/src/compaction_stats.rs:27-72 (CompactionStatsAtomic), crates/anie-cli/src/controller.rs:1408-1430 (emit_compaction_end increments), crates/anie-cli/src/controller.rs:2258-2266 (format_state_summary renders snapshot)",
        "claim": "CompactionStats (compaction_stats.rs) tracks pre_prompt, mid_turn, reactive_overflow counts per session and surfaces via `/state` command. This is visible only to the user in a live session. No centralized collection pipeline, no trending over time, no alert on anomalies (e.g. 'mid-turn compaction firing 10x more often than last week').",
        "rival_baseline": "A fleet-grade rival would export: session_id, harness_mode, model, compaction_phase, tokens_freed, duration_ms to a metrics store (InfluxDB, Prometheus, BigQuery) so analysts can ask: 'Which models hit mid-turn compaction most? Does RLM mode reduce reactive compactions on small models?' without manual session file inspection."
      }
    ],
    "partial": [
      {
        "id": "ARCH-1",
        "lens": "sandbox-approvals",
        "title": "BeforeToolCallHook trait is defined but infrastructure for user-facing approvals is not wired",
        "severity": "high",
        "impact": 5,
        "effort": 4,
        "corrected_impact": 3,
        "corrected_effort": 4,
        "reasoning": "The BeforeToolCallHook infrastructure exists but is incomplete and test-only. The hook trait is defined (hooks.rs:38-46), integrated into the agent loop (agent_loop.rs:1466-1486), and can block tool calls by returning BeforeToolCallResult::Block. However: (1) There is NO public API to install a hook—only a test-only with_hooks method (agent_loop.rs:487, cfg(test)). (2) No controller code installs any hook (grep -r \"with_before_tool_call\" returns nothing). (3) Zero user-facing approval infrastructure: no TUI modal code, no AgentEvent variant for approval requests, no permission-checking logic wired to the controller. (4) The claim mischaracterizes the plan: Phase 3 of plan 10 (10_extension_system_pi_port.md:383-398) does NOT mention \"approval prompts\"—it only says extensions can block tool calls. Approval dialogs (confirm/select) are planned for Phase 5 (lines 560-624) as general UI primitives extensions can invoke, not as native anie approval infrastructure. Real approval UX would require: a public with_before_tool_call_hook setter, a policy impl in the controller that decides when to prompt, a TUI overlay for user interaction, and wiring through AgentEvent. None of these exist.",
        "scaffolding": "hooks.rs:38-46 (BeforeToolCallHook trait), agent_loop.rs:369-370 (field in AgentLoopConfig), agent_loop.rs:487-497 (test-only with_hooks setter), agent_loop.rs:1466-1486 (hook invocation in tool execution path), BeforeToolCallResult::Block variant (hooks.rs:23)",
        "claim": "The hook exists (agent_loop.rs:369, executed at line 1466-1486) and can Block a tool call, but there is no: (1) UI modal/prompt to surface approvals to users, (2) controller code to install a real hook impl, (3) plan for interactive approval UX. This is planned for Phase 3 of the extension system (docs/refactor_plans/10_extension_system_pi_port.md:383-398).",
        "rival_baseline": "Claude Code: TUI controller presumably has ApprovalModal or similar to prompt user when permission=ask. Codex (codex_summary.md:196-198): 'agent emits ExecApprovalRequestEvent. The client (TUI) shows a modal; the user can approve once, approve a session-scoped allow rule, or deny.'"
      },
      {
        "id": "POLICY-1",
        "lens": "sandbox-approvals",
        "title": "No permission model at config or runtime level for whitelisting/blocking tools",
        "severity": "high",
        "impact": 4,
        "effort": 3,
        "corrected_impact": 4,
        "corrected_effort": 3,
        "reasoning": "Hook infrastructure scaffolding exists (BeforeToolCallHook trait in crates/anie-agent/src/hooks.rs:17-46, integrated at agent_loop.rs:1466-1486) but is never instantiated. Default is None (agent_loop.rs:403, 494). No code anywhere calls set_before_tool_call_hook. Config layer (crates/anie-config/src/lib.rs) has only BashPolicyConfig (lines 157-183) with deny_commands/deny_patterns—no per-tool permission schema. Only --no-tools (bootstrap.rs:138-140) offers granular control, disabling all tools globally. README.md:39 explicitly documents \"tools run without sandboxing or approvals.\" The hook trait is production-ready but requires external implementation (deferred to plan 10 JSON-RPC extension system). This is partial because the plumbing exists but the policy engine is absent.",
        "scaffolding": "crates/anie-agent/src/hooks.rs:17-46, agent_loop.rs:1466-1486, 1807-1815 (before_model_policy pattern exists as reference)",
        "claim": "anie-config has no permission/allowlist/deny-list schema for specific tools (bash, write, edit, read). Only bash has a deny_commands/deny_patterns policy in [tools.bash.policy]; no equivalent for other tools. No role-based or capability-based tool gating. All tools are registered and available if the session is active.",
        "rival_baseline": "Claude Code: unknown detailed model, but settings.json supports permissions.defaultMode (ask/allow/deny). Codex (codex_summary.md:179-194): '.rules' files under ~/.codex/rules/ define prefix patterns per executable with Allow/Deny/AskForApproval decisions; network rules separate. System-wide + per-project scopes."
      },
      {
        "id": "MCP-GAP-4",
        "lens": "mcp",
        "title": "No async subscription / streaming response support in tool execution",
        "severity": "medium",
        "impact": 2,
        "effort": 3,
        "corrected_impact": 3,
        "corrected_effort": 3,
        "reasoning": "anie DOES support streaming tool result updates via mpsc::Sender<ToolResult> channels (crates/anie-agent/src/agent_loop.rs:1488-1500, with active usage in bash and web_read tools). The update_tx parameter allows tools to emit multiple partial ToolResult messages during execution, which are forwarded to AgentEvent::ToolExecUpdate events. However, the claim is partially correct that anie lacks bidirectional subscription support - there is NO mechanism for external servers to maintain subscriptions or push unsolicited events (no MCP Notification handling, no server-initiated events, no long-polling infrastructure). The gap is specifically about server-to-client push patterns and external subscription management, NOT about streaming responses from tools. The claim's title overstates the gap by implying streaming support doesn't exist when it demonstrably does via the update channel pattern.</reasoning>\n<parameter name=\"existing_scaffolding\">crates/anie-agent/src/agent_loop.rs:1488-1512 (mpsc channel for tool updates, forwarder task), crates/anie-protocol/src/events.rs:55-58 (AgentEvent::ToolExecUpdate variant), crates/anie-agent/src/tool.rs:119 (update_tx parameter in Tool::execute signature), crates/anie-tools/src/bash.rs:172-174 (actual usage sending partial updates)",
        "claim": "Tool execution is request-response only. The agent loop awaits `Tool::execute()` (crates/anie-agent/src/tool.rs:114-121) with optional streaming over `update_tx: Option<mpsc::Sender<ToolResult>>`. MCP's samples resource stream and long-polling patterns are not supported. External servers cannot maintain bidirectional subscriptions or push updates asynchronously.",
        "rival_baseline": "MCP spec allows servers to emit Notifications (push events) over the JSON-RPC transport. Codex's `rmcp-client` and event routing would handle these. anie cannot receive or route unsolicited server messages."
      },
      {
        "id": "RECURSE-2",
        "lens": "agentic-planning-verify",
        "title": "Recurse depth and budget are shallow (depth 2, 8 calls)",
        "severity": "medium",
        "impact": 2,
        "effort": 2,
        "corrected_impact": 3,
        "corrected_effort": 2,
        "reasoning": "The claim about recursion depth being \"shallow (depth 2, 8 calls)\" is partially accurate but overstated. Actual state:\n\n**WHAT EXISTS (confirmed in code):**\n- Max depth is hardcoded to 2: `const RECURSION_MAX_DEPTH: u8 = 2;` (crates/anie-cli/src/controller.rs:117)\n- Default budget is hardcoded to 8: `const RECURSION_BUDGET_DEFAULT: u32 = 8;` (crates/anie-cli/src/controller.rs:108)\n- These are shared atomics (Arc<AtomicU32>) across sub-calls (crates/anie-tools/src/recurse.rs:54)\n- Recursion only works in `--harness-mode=rlm` (crates/anie-cli/src/harness_mode.rs:70-73)\n- Sub-agents at depth 0 can recurse once (depth 1), creating depth-2 sub-agents that cannot recurse (no recurse tool in registry)\n\n**WHAT'S MISSING (genuine gap):**\n1. No environment variable override for `RECURSION_BUDGET_DEFAULT` or `RECURSION_MAX_DEPTH`. Contrast with available overrides like `ANIE_ACTIVE_CEILING_TOKENS` (line 1880), `ANIE_KEEP_LAST_N` (line 1888), `ANIE_RELEVANCE_BUDGET_TOKENS` (line 1902).\n2. Plan 06 (phased_implementation.md) describes Phase A as complete but explicitly notes: \"rlm/05 ships sub-agents without any tools at all, so this is effectively only enforced at the top-level → depth-1 boundary; deeper depths are inaccessible until a follow-up commit registers recurse on sub-agents\" (controller.rs:112-115). Re-enabling recursion on depth-2 agents requires that follow-up commit.\n3. Budget of 8 is explicitly marked as \"tighter starting value ... so we hit budget exhaustion quickly during testing\" (line 106) — not a production target.\n\n**VERDICT RATIONALE:**\nThis is \"partial\" not \"confirmed_gap\" because: (a) the recursion constraints ARE implemented (depth and budget exist and work), (b) they're intentionally conservative per the design doc, and (c) the scaffolding for making them configurable (environment-variable-driven config pattern) is already established in the codebase just not wired to recursion yet. The claim's severity (\"prevents deep task hierarchies or large fan-outs\") is accurate for the *current design*, but the gap is configurable defaults + re-enabling depth-2 recursion in a follow-up commit, not architectural.\n\nThe claim that \"no explicit budget config found in CLI\" is literally true (grep 'recursion_budget' crates/anie-cli/src/*.rs yields only context_virt.rs references), but this is by design: budget initialization happens in controller.rs via the hardcoded constant, not the config system.",
        "scaffolding": "crates/anie-cli/src/controller.rs:117 (RECURSION_MAX_DEPTH), crates/anie-cli/src/controller.rs:108 (RECURSION_BUDGET_DEFAULT), crates/anie-cli/src/controller.rs:1878-1910 (env var pattern for rlm settings)",
        "claim": "RecurseTool hardcodes max_depth in SubAgentBuildContext (agent/src/recurse.rs:126-145) and shares a global budget counter (line 54: `Arc<AtomicU32>`). Default budget is 8 (SPECULATIVE from line 172-173 error message). Prevents deep task hierarchies or large fan-outs.",
        "rival_baseline": "Claude Code's sub-agent spawn allows configurable depth and per-run budget scaling with context window. Parallel swarming may use higher budget (N agents × budget = total). SPECULATIVE: Codex depth may be 3+ with per-agent budgets."
      },
      {
        "id": "EDIT-3",
        "lens": "edit-reliability",
        "title": "Fuzzy whitespace fallback is undocumented; no preview before fuzzy match",
        "severity": "high",
        "impact": 2,
        "effort": 2,
        "corrected_impact": 3,
        "corrected_effort": 2,
        "reasoning": "The fuzzy whitespace fallback EXISTS and IS FUNCTIONAL (lines 275-299 in edit.rs implement it correctly, with normalize_for_fuzzy_match at 423-453 collapsing whitespace runs). However, the CLAIM about undocumented/opaque behavior is CONFIRMED.\n\nSCAFFOLDING PRESENT:\n- Fuzzy matching logic: crates/anie-tools/src/edit.rs:275-299 (fallback triggered on exact-match failure)\n- Normalization: lines 423-453 (collapses consecutive whitespace except newlines to single spaces)\n- Error handling: lines 282-292 (returns errors on 0 or 2+ fuzzy matches)\n- Test coverage: crates/anie-tools/src/tests.rs:1371-1398 (unit test confirms fuzzy works)\n\nGAPS CONFIRMED (Documentation/UX):\n1. Tool description (line 58) says \"Edit a single file using exact text replacement\" — NO mention of fuzzy fallback, whitespace-insensitive matching, or fallback behavior\n2. Parameter description (line 70): \"Exact text for one targeted replacement\" — contradicts reality\n3. No signal to user when fuzzy succeeds: lines 135-147 return only message \"Applied N edits\" and diff, with no indication fuzzy was used\n4. Error messages are generic on fuzzy failure (line 283-285: \"did not match anything\" doesn't say fuzzy failed)\n5. No preview/confirmation flow: diff is applied directly without intermediate \"fuzzy matched here\" notification (line 318: render_diff just outputs final result)\n6. Test is unit-level only, not CLI-integrated (directly calls tool.execute, not through agent loop or model tool calling context)\n\nSEVERITY: Medium (matches rival analysis doc phase1_raw.json assessment). The functionality works correctly, but users have no visibility into when fuzzy fallback activates, potentially causing surprise matches or confusion about what happened.\n\nEFFORT TO FIX: Low (2-3, not 1-5). Update tool description string, add success-case message indicating fuzzy was used, possibly add optional confirmation step.",
        "scaffolding": "crates/anie-tools/src/edit.rs:275-299 (fuzzy fallback), 423-453 (normalize_for_fuzzy_match), 282-292 (error handling); crates/anie-tools/src/tests.rs:1371-1398 (unit test)",
        "claim": "When exact text matching fails, the edit tool silently falls back to fuzzy matching (collapsing whitespace runs to single spaces). This fallback is not mentioned in the tool description or error messages, and there is no preview/confirmation shown to the user before the fuzzy match is applied.",
        "rival_baseline": "Codex: context-based hunk matching is explicit — the patch format includes context lines, and the tool reports whether a hunk matched exactly or had to be fuzzy-matched. Claude Code: unknown; likely exact-only with explicit error on mismatch."
      },
      {
        "id": "SESSION-2",
        "lens": "session-ux",
        "title": "Session tree overlay is a stub — no branch visualization",
        "severity": "medium",
        "impact": 2,
        "effort": 4,
        "corrected_impact": 2,
        "reasoning": "TreeScreen stub confirmed at crates/anie-tui/src/overlays/tree.rs:1-40, explicitly marked \"not yet implemented\" with render_placeholder_panel. However, session branching infrastructure partially exists: (1) parent_session field persists in SessionHeader (anie-session/src/lib.rs:108), (2) fork_to_child_session() implementation creates child sessions with parent links (anie-session/src/lib.rs:613-630), (3) /fork command works. Gap is in visibility and UI: SessionInfo struct (anie-session/src/lib.rs:199-209) lacks parent_session field—list_sessions() reads but discards it from header (line 1002-1010). No tree visualization, no branch-aware listing, no tree navigation UI exists. Users CAN fork/branch but CANNOT see relationships visually. This is partial because scaffolding exists (data layer) but UI discovery layer is entirely absent.</reasoning>\n<parameter name=\"existing_scaffolding\">crates/anie-tui/src/overlays/tree.rs:19-40 (TreeScreen stub struct); crates/anie-session/src/lib.rs:108 (parent_session in SessionHeader); crates/anie-session/src/lib.rs:613-630 (fork_to_child_session implementation)</parameter>\n<parameter name=\"corrected_effort\">3",
        "claim": "No visual representation of session branching tree; users cannot see fork/branch relationships or navigate the tree.",
        "rival_baseline": "pi (Claude Code docs) supports branch tracking via parent_id and createBranchedSession; docs/anie_vs_pi_comparison.md:378-391 documents pi's branchWithSummary and branch-leaving automatic summarization. Codex uses SQLite threads with parent thread links visualizable in CLI."
      },
      {
        "id": "PROVIDER-1",
        "lens": "provider-breadth-cost",
        "title": "Only 3 of 5 ApiKind enums are implemented",
        "severity": "high",
        "impact": 4,
        "effort": 3,
        "corrected_impact": 3,
        "corrected_effort": 4,
        "reasoning": "GoogleGenerativeAI has zero scaffolding — no Provider impl, no model discovery support (explicitly errors), and is explicitly tested as unregistered (crates/anie-integration-tests/tests/config_wiring.rs:19-21). OpenAIResponses has partial scaffolding: it's routed in model_discovery.rs:45 alongside OpenAICompletions, but there is NO Provider implementation registered (crates/anie-providers-builtin/src/lib.rs:50-57 shows only 3 registered: AnthropicMessages, OpenAICompletions, OllamaChatApi). Attempting to use OpenAIResponses would fail at runtime with \"No provider registered\" since it has no Provider impl and isn't in the registry. Model discovery can attempt to discover models for OpenAIResponses (treating it like OpenAICompletions), but this scaffolding is non-functional without a registered Provider. The 3-of-5 claim is correct: only AnthropicMessages, OpenAICompletions, and OllamaChatApi have Provider implementations. Plans for Gemini/Responses/Bedrock/etc are documented as deferred in docs/add_providers/README.md with sole focus on OpenRouter.",
        "scaffolding": "crates/anie-providers-builtin/src/model_discovery.rs:45 (routes OpenAIResponses to openai_compatible discovery, but no Provider registered); crates/anie-integration-tests/tests/config_wiring.rs:19-21 (explicitly asserts GoogleGenerativeAI not registered)",
        "claim": "OpenAIResponses and GoogleGenerativeAI are defined in ApiKind but have no Provider implementation; only AnthropicMessages, OpenAICompletions, and OllamaChatApi are registered.",
        "rival_baseline": "pi ships 15 Api values (openai-completions, openai-responses, openai-codex-responses, azure-openai-responses, anthropic-messages, bedrock-converse-stream, google-generative-ai, google-gemini-cli, google-vertex, mistral-conversations, plus ~5 others). pi's github, azure-openai, xai, groq, cerebras integrations are all shipped."
      },
      {
        "id": "DISCOVERY-1",
        "lens": "provider-breadth-cost",
        "title": "Model discovery is unimplemented for most future providers",
        "severity": "medium",
        "impact": 2,
        "effort": 3,
        "corrected_impact": 2,
        "corrected_effort": 2,
        "reasoning": "The claim significantly understates what model discovery scaffolding already exists. Breaking down the actual state:\n\nIMPLEMENTED AND WORKING:\n1. OpenAI-compatible discovery (ApiKind::OpenAICompletions + OpenAIResponses) at crates/anie-providers-builtin/src/model_discovery.rs:322-463. This covers:\n   - OpenRouter (with specific tool-filtering for non-agent models) - lines 354-373\n   - OpenAI - via the same discovery function\n   - GitHub Copilot (model_picker_enabled filtering) - lines 374-399\n   - Local OpenAI-compatible servers (Ollama OpenAI mode)\n   Tests at lines 1442-1556 and 1514-1557 exercise OpenRouter and Copilot discovery specifically.\n\n2. Anthropic discovery (ApiKind::AnthropicMessages) at lines 465-511.\n\n3. Ollama native discovery (ApiKind::OllamaChatApi) at lines 513-643 with parallel /api/show capability probing and comprehensive tests.\n\n4. ModelDiscoveryCache with TTL and refresh semantics at lines 56-121 (shared infrastructure).\n\n5. Integration into onboarding at crates/anie-tui/src/overlays/onboarding.rs and models_command.rs.\n\n6. OpenRouter capability mapping function (apply_openrouter_capabilities) at crates/anie-providers-builtin/src/openrouter.rs - wires upstream-aware reasoning/replay capabilities based on model ID prefix pattern-matching.\n\nNOT IMPLEMENTED:\n- Google Generative AI (ApiKind::GoogleGenerativeAI) explicitly returns RequestBuild error at line 49-51. This is acknowledged as deferred per plan 03.\n- Amazon Bedrock (ApiKind::BedrockConverseStream) - not yet in the enum, deferred to plan 06.\n\nDEFERRED PLANS BUT NOT A BLOCKER:\n- Plan 02 (xAI, Groq, Mistral) all use ApiKind::OpenAICompletions, so they automatically inherit the discovery infrastructure once provider modules are written. Discovery itself is not the blocker.\n- Plan 04 (OpenAI Responses API) uses ApiKind::OpenAIResponses, already covered by the OpenAI-compatible discovery.\n- Plan 05 (Azure OpenAI) uses ApiKind::OpenAIResponses, same coverage.\n\nThe claim's characterization as \"Ollama only\" and \"OpenRouter will be added\" is outdated. OpenRouter discovery is fully implemented and tested. The real gaps are Google Gemini (explicitly unimplemented, acknowledged in code) and Bedrock (deferred API kind). Both are known and documented in the plans. The OpenAI-compatible discovery pathway already handles xAI/Groq/Mistral at the protocol level; they just need provider registration and auth configuration.",
        "scaffolding": "crates/anie-providers-builtin/src/model_discovery.rs:40-54 (discover_models entry point), :322-463 (OpenAI-compatible with OpenRouter filtering), :465-511 (Anthropic), :513-643 (Ollama); crates/anie-providers-builtin/src/openrouter.rs (apply_openrouter_capabilities); crates/anie-tui/src/overlays/onboarding.rs (discovery integration); tests at lines 1442-1556, 1514-1557, 1164-1222",
        "claim": "Live model discovery exists only for Ollama (crates/anie-providers-builtin/src/local.rs) and will be added for OpenRouter (docs/add_providers/01_openrouter.md). No discovery scaffolding for Google Gemini, xAI, Groq, Bedrock, or Azure — each deferred plan lacks discovery implementation.",
        "rival_baseline": "pi auto-generates models.generated.ts from provider APIs (pi_summary.md:81). Every provider pi supports has a discovery or catalog-fetch path. anie ships only Ollama discovery; OpenRouter discovery is planned but not merged."
      },
      {
        "id": "BREADTH-1",
        "lens": "provider-breadth-cost",
        "title": "OpenRouter is reachable but unshipped; sub-agent spawn is serial-only with depth 2 hard limit",
        "severity": "low",
        "impact": 2,
        "effort": 3,
        "corrected_impact": 2,
        "corrected_effort": 2,
        "reasoning": "OPENROUTER STATUS: REFUTED. OpenRouter is shipped and merged to main—not \"unshipped.\" Commits on main: d7872c7 (M0 PR A: Model.compat blob), d38b369 (M1 PR A: preset+discovery), 2c6609b (M1 PR B part 1: capability mapping), 370cc2b (M1 PR C: fuzzy scoring). Full implementation is live.\n\nRECURSION DEPTH LIMIT: CONFIRMED but context-incomplete. The depth limit (RECURSION_MAX_DEPTH = u8 = 2) is hardcoded in crates/anie-cli/src/controller.rs:117, and the recurse tool is serial-only (executes one sub-agent sequentially via `while !machine.is_finished() { machine.next_step() }`; see crates/anie-tools/src/recurse.rs:218-220). However:\n\n1. **Depth 2 is intentional phasing, not an architectural hard limit.** The comment (line 110-116) explicitly states: \"Note: `rlm/05` ships sub-agents without any tools at all, so this is effectively only enforced at the top-level → depth-1 boundary; deeper depths are inaccessible until a follow-up commit registers recurse on sub-agents.\" This is a deliberate staging choice, not a limitation.\n\n2. **Budget 8 is correct** (RECURSION_BUDGET_DEFAULT = u32 = 8; line 116, controller.rs).\n\n3. **\"Parallel sub-agent fork support\" claim is misleading.** The comparison doc (docs/anie_vs_pi_comparison.md:270-274) never claims pi's extensions support parallel sub-agent spawning—it only notes pi's extension system is \"more flexible.\" Anie DOES support parallel tool execution at the normal agent level (crates/anie-agent/src/agent_loop.rs uses `join_all(futures)` when ToolExecutionMode::Parallel; test: parallel_tool_calls_execute_concurrently). The gap is not \"parallel sub-agents\" (which is an exotic use case), but rather whether the recurse tool itself could spawn multiple sub-agents concurrently—currently it doesn't, by design.",
        "scaffolding": "crates/anie-tools/src/recurse.rs (full tool, serial execution) | crates/anie-cli/src/controller.rs:117 (RECURSION_MAX_DEPTH = 2) | crates/anie-agent/src/agent_loop.rs (parallel tool execution via join_all)",
        "claim": "OpenRouter planning is thorough (docs/add_providers/) but not merged; sub-agent recursion (crates/anie-tools/src/recurse.rs) is serial-only, depth ≤ 2, with budget 8 — no parallel spawn like pi's extension system could support.",
        "rival_baseline": "pi's extension system (anie_vs_pi_comparison.md:270-274) allows arbitrary extension code to wrap tools; extensions can spawn parallel work. anie's RecurseTool is a single tool that serializes sub-agent calls into the main loop."
      },
      {
        "id": "EVAL-2",
        "lens": "eval-observability",
        "title": "No structured metrics export (token usage, latency, tool success rate) to JSON or database",
        "severity": "high",
        "impact": 4,
        "effort": 2,
        "corrected_impact": 3,
        "corrected_effort": 3,
        "reasoning": "The claim is **partially correct**. Anie has scaffolding for per-session metrics but lacks any centralized aggregation, JSON export, or queryable analytics:\n\n**Existing scaffolding (confirmed):**\n- `anie-protocol/src/usage.rs`: Usage struct with input_tokens, output_tokens, cache_read/write_tokens, and Cost (file:line 1-35)\n- `anie-protocol/src/events.rs`: CompactionEnd variant with tokens_before, tokens_after, phase field (file:line with CompactionEnd definition)\n- `anie-session/src/lib.rs`: Session entries persist Usage per AssistantMessage (file:line 129-155 shows SessionEntry variants including Message and Compaction)\n- `crates/anie-cli/src/compaction_stats.rs`: CompactionStatsAtomic struct tracking pre_prompt, mid_turn, reactive_overflow counts per session (file:line 26-80). DISPLAY-ONLY on `/state` command (file:line ~1150 in controller.rs shows format_state_summary includes compaction stats rendering)\n\n**Confirmed ABSENT:**\n- No JSON export command or flag to dump session metrics\n- No SQLite/CSV database sink for cross-session trending\n- No metrics aggregation tool (e.g., `anie analyze-sessions --output=metrics.json`)\n- No structured telemetry sink (Datadog, Honeycomb, etc.)\n- No `/compaction-stats` subcommand (deferred in plan 06, line 84-95, section \"Deferred\" lines 244-245: \"Persistent metrics across sessions... speculative; nice-to-have\")\n- No tool for querying past sessions: \"token_usage_avg.json\", \"retry_rate_by_provider.json\", etc.\n- Compaction stats are **volatile** (in-process, not persisted); resuming a session via `--continue` resets counters to zero (plan 06 notes this as anie-specific deviation: file `/home/daniel/Projects/agents/anie/docs/midturn_compaction_2026-04-27/06_compaction_telemetry.md` line ~97: \"counters are this-process-lifetime, not durable\")\n\n**Gap summary:** Data *exists* in the session JSONL and in-memory atomics, but there is **zero post-hoc aggregation or export layer**. A user cannot easily answer: \"What's my average session token cost?\" or \"How much time do I spend on tool retries across all sessions?\" — there's no command, no tool, no sink that reads the session logs and produces queryable metrics.",
        "scaffolding": "anie-protocol/src/usage.rs:1-35 (Usage struct); anie-protocol/src/events.rs (CompactionEnd with token deltas); anie-cli/src/compaction_stats.rs:26-80 (CompactionStatsAtomic); anie-cli/src/controller.rs:1150-1180 (format_state_summary displays stats on /state); docs/midturn_compaction_2026-04-27/06_compaction_telemetry.md:244-253 (defers persistent metrics as future work)",
        "claim": "AgentEvent enum captures `CompactionEnd` with tokens_before/tokens_after, and Session schema stores Usage (input_tokens, output_tokens, cost). However, there is no centralized metrics collector that aggregates these per-run into a queryable format (JSON, SQLite, CSV) for post-session analysis or cross-session trending.",
        "rival_baseline": "SPECULATIVE: Claude Code likely has structured logging or an events sink (Datadog, Honeycomb, or similar) that records model quality signals (token usage, latency, tool success, retry rates) for fleet observability. This enables post-hoc analysis: 'baseline mode averages 45k tokens; RLM mode 28k; latency +33%.'"
      },
      {
        "id": "EVAL-4",
        "lens": "eval-observability",
        "title": "Harness-mode flag implemented but baseline-vs-current-vs-rlm comparison framework incomplete",
        "severity": "high",
        "impact": 4,
        "effort": 3,
        "corrected_impact": 2,
        "corrected_effort": 4,
        "reasoning": "The HarnessMode enum (anie-cli/src/harness_mode.rs:1-122) and StatusUpdate integration (anie-protocol/src/events.rs:87) are fully implemented and tested. The `--harness-mode {baseline,current,rlm}` flag correctly gates capabilities as designed. However, the evaluation framework promised in Plan 07 exit criteria is almost entirely absent: no anie-evals crate, no scenario TOML corpus, no scenario runner, no automated/rubric scoring, no multi-run aggregation, and no baseline measurements. Plan 07 is marked \"partial\" in the execution tracker with \"scenarios deferred\". The harness-mode plumbing is production-ready; the measurement infrastructure needed to validate the RLM paradigm (baseline: score X, tokens Y; current: score Z, tokens W; rlm: score V, tokens U; delta reporting) does not exist.",
        "scaffolding": "anie-cli/src/harness_mode.rs (122 LOC, tested); anie-protocol/src/events.rs:87 (StatusUpdate.harness_mode); controller integration in bootstrap.rs, context_virt.rs, controller_tests.rs with mode-specific tests (build_rlm_extras_only_installs_recurse_in_rlm_mode, compose_system_prompt_only_augments_in_rlm_mode)",
        "claim": "HarnessMode enum (anie-cli/src/harness_mode.rs) correctly gates baseline/current/rlm capabilities. StatusUpdate event surfaces harness_mode label. However, there is no test suite or scenario runner that compares the same prompt across all three modes and reports: 'baseline: score X, tokens Y; current: score Z, tokens W; rlm: score V, tokens U; delta rlm vs current: +A% quality, -B% cost.'",
        "rival_baseline": "A rival would have run initial measurements before landing Plan 06. For example: 'On 10 long-context scenarios, RLM mode improves rubric by 2.1 points (rlm 4.2 vs current 2.1) at cost of +28% latency and -20% token usage.' This establishes whether the complexity is justified."
      }
    ],
    "dropped": [
      {
        "id": "PATH-1",
        "lens": "sandbox-approvals",
        "title": "No path boundary enforcement — absolute paths and parent traversal allowed by design",
        "severity": "high",
        "impact": 4,
        "effort": 3,
        "corrected_impact": 5,
        "corrected_effort": 4,
        "reasoning": "No path boundary enforcement exists in anie, and this is explicitly intentional. The claim is accurate: `resolve_path()` in shared.rs:42-53 passes absolute paths through unchanged and allows parent traversal via `..` by design. The test at lines 161-168 documents this intentionality. All file access tools (read, edit, ls, grep, find, bash) use this same function with zero boundary checks. ToolExecutionContext (anie-agent/src/tool.rs) has no permission fields — it only carries `context_window`. The README explicitly states: \"tools currently run without sandboxing or approvals... absolute paths and .. traversal are allowed.\" This is not a bug or oversight; it is acknowledged architecture. BashPolicy is explicitly marked as \"not a sandbox\" and only catches accidental misuse (deny_commands, deny_patterns). There is no allow-list enforcement anywhere in the codebase. The claim correctly identifies this as a gap in security posture, but the gap already exists and is documented.",
        "scaffolding": "/home/daniel/Projects/agents/anie/crates/anie-tools/src/shared.rs:42-53 (resolve_path function), lines 161-168 (test_resolve_path_preserves_parent_traversal), /home/daniel/Projects/agents/anie/README.md:39 (documentation of intentional design)",
        "claim": "shared.rs:42-53 resolve_path() explicitly preserves absolute paths and parent (..) traversal. Test at line 161-168 documents this is intentional ('resolve_path_preserves_parent_traversal'). Tools have same filesystem access as anie process user, cwd is advisory only.",
        "rival_baseline": "Claude Code: permission modes ('ask'/'allow'/'deny') gate reads/writes after path resolution, but design unclear from visible config. Codex (codex_summary.md:109-110): TurnContext captures 'sandbox permissions derived from config' and 'file-system sandbox context (cwd, allowed_paths)' — implies allow-list enforcement during ExecutorFileSystem.write checks."
      },
      {
        "id": "EDIT-6",
        "lens": "edit-reliability",
        "title": "BOM and CRLF preservation is robust, but line-ending logic is single-pass string replacement",
        "severity": "low",
        "impact": 1,
        "effort": 1,
        "corrected_impact": 1,
        "corrected_effort": 1,
        "reasoning": "The claim asserts that the CRLF restoration logic is vulnerable to double-escaping if newText contains literal CRLF sequences. However, this vulnerability does not exist because the code normalizes BOTH oldText AND newText to LF during parse_edits (crates/anie-tools/src/edit.rs:219-220), which occurs BEFORE apply_edits is called. The full cycle is: (1) Detect original line ending (line 117), (2) Normalize original file to LF (line 118), (3) Parse edits with BOTH old_text and new_text normalized to LF (lines 219-220), (4) Apply normalized edits to normalized content (line 119), (5) Restore original line ending (line 120). This ensures that even if an LLM produces newText with embedded CRLF sequences, they are normalized to LF before being inserted, then correctly restored to the original line ending. The test at lines 1337-1368 in crates/anie-tools/src/tests.rs verifies BOM and CRLF preservation, though it does not explicitly test the edge case of newText containing embedded newlines on a CRLF file—but the code handles this correctly regardless.",
        "scaffolding": "crates/anie-tools/src/edit.rs:219-220 (newText normalization), 117-120 (full cycle), 1337-1368 in crates/anie-tools/src/tests.rs (CRLF preservation test)",
        "claim": "anie correctly detects and preserves BOM and line endings (CR/LF/CRLF), and normalizes to LF during edit, then restores. However, the restore step (line 393) uses simple string replace, which could theoretically create unexpected results if the newText contains literal CRLF sequences that should not be doubled.",
        "rival_baseline": "SPECULATIVE: Codex and Claude Code likely use similar normalize-then-restore patterns. Not a major issue; edge case only when LLM produces CRLF-escaped newText."
      },
      {
        "id": "SESSION-6",
        "lens": "session-ux",
        "title": "No split-turn compaction entry type or file-operation provenance tracking",
        "severity": "low",
        "impact": 1,
        "effort": 4,
        "corrected_impact": 0,
        "corrected_effort": 0,
        "reasoning": "Both split-turn compaction and file-operation provenance tracking are fully implemented in the codebase. SplitTurn struct (line 1444) and split_turn field in CutPoint (line 1436) handle mid-turn cuts. CompactionDetails struct (line 257) with read_files/modified_files tracks file operations. extract_compaction_details() (line 287) extracts paths from tool calls. append_details_to_summary() (line 338) appends details as XML tags. SessionEntry::Compaction (line 138-154) persists details. compact_internal() (lines 1046-1076) implements parallel summarization for split turns using futures::try_join!, matching pi's format with join_split_turn_prose() (line 1523). These features were implemented on 2026-04-21 (commit d8bf578, bced0ce, 90c9c77, 6876d42), four days before the comparison doc was written (2026-04-25) and the doc was never updated. Tests confirm both features work: find_cut_point_surfaces_split_turn_when_cut_lands_in_turn() (2613), extract_compaction_details_picks_up_read_and_write_paths() (2270).",
        "scaffolding": "file:crates/anie-session/src/lib.rs:1444-1450 (SplitTurn struct), file:crates/anie-session/src/lib.rs:1436 (split_turn field in CutPoint), file:crates/anie-session/src/lib.rs:257-267 (CompactionDetails struct), file:crates/anie-session/src/lib.rs:287-312 (extract_compaction_details function), file:crates/anie-session/src/lib.rs:338-360 (append_details_to_summary function), file:crates/anie-session/src/lib.rs:138-154 (Compaction variant with details field), file:crates/anie-session/src/lib.rs:1046-1076 (compact_internal with split-turn branching and parallel summarization)",
        "claim": "Compaction does not track which files were modified during the discarded interval, and does not preserve context when the cut lands mid-turn (pi's split-turn handling).",
        "rival_baseline": "pi tracks split_turn: Option<SplitTurn> in CutPoint and generates two summaries (main + prefix). pi's compaction details include file_operations array. Codex does not surface split-turn compaction in docs."
      },
      {
        "id": "REASONING-1",
        "lens": "provider-breadth-cost",
        "title": "Reasoning field name normalization misses one field variant",
        "severity": "low",
        "impact": 1,
        "effort": 1,
        "corrected_impact": 0,
        "corrected_effort": 0,
        "reasoning": "The `reasoning_text` field name is fully implemented in native_reasoning_delta() at crates/anie-providers-builtin/src/openai/reasoning_strategy.rs:254-269, which checks [\"reasoning\", \"reasoning_content\", \"reasoning_text\", \"thinking\"] in order. Comprehensive test coverage exists at reasoning_strategy.rs:317-332 (native_reasoning_delta_captures_every_known_field_name) that explicitly tests all four field names including reasoning_text. Implementation was committed 2026-04-20 in \"M1 PR B (part 1)\" and is on main. The plan doc (docs/add_providers/01_openrouter.md:127-130) correctly identifies it as \"Missing\" as a planning artifact, but the feature has been implemented and tested. Claim is based on stale documentation without verifying the actual code.",
        "claim": "OpenRouter upstreams emit reasoning in three possible delta field names; anie checks 'reasoning' and 'reasoning_content' but not 'reasoning_text' (used by some OpenRouter upstreams, per pi).",
        "rival_baseline": "pi at openai-completions.ts:186-226 checks all three field names."
      }
    ]
  }
}